# 慧尖语音助手 · 落地方案 v4（基线重构版 · 2026-09-08）

> **取代 v3 成为执行基线。** 触发重构的两件事：
> ① 用户提供语音集成最新源码 `yyjicheng/`（domain=`huijian_ai` v1.1.0）——HA 侧大脑已建成 80%；
> ② 用户明示"**固件也需要跟着修改**"——固件从"零改动约束"转为"改造资产"。
> 契约细节全部收录于同目录《语音集成源码盘点-huijian_ai-2026-09-08.md》（下称【盘点】），本文只写方案，缺陷编号 D1-D13 引用【盘点】§6。
> v3 仍有效的部分：fast_path/TextCNN 收编清单与 3 缺陷、模型选型与 sid45 默认、配置体系三层入口、云厂商三家清单、存量不迁移原则。

---

> **v4.1 同日修订（用户三确认，2026-09-08 晚）**：
> ① **公网小智服务器从未上线、无存量语音客户 → 彻底退役，模式 A 全线取消**——本文所有"存量兼容/回迁/双模仲裁/M4 承接"条款作废；固件只做卫星形态（xiaozhi WS 留作调试后门不进产品路径）；小程序不接云函数下发；"现网容器 diff 收编 fast_path"降级为"以备份源码+文档为准"。
> ② **STT/TTS/LLM 三项均用户可配**：STT/TTS 默认本地模型（可切云，故障自动回落）；LLM 默认**关闭**（4C8G 不存在可用本地 LLM；关闭态 fast_path+查询族+场景全功能，miss 走固定兜底），开启=OpenAI 兼容端点（云端百炼/火山方舟[可预置慧尖 SFT 模板]或 LAN ollama）。
> ③ **加载项形态定案：单独新建 `huijian_voice` 加载项，不并入 LoRa 网关加载项**。理由：镜像体积（ghcr 家宽拉取实测约束，语音栈并入翻倍）、重启爆炸半径（语音侧操作不能带走 Mosquitto=全屋窗设备离线）、发布节奏解耦、8GB 机器资源隔离与可停可卸、**产品边界——语音不依赖网关，执行走 `/api/intent/handle` 控制 HA 全部设备，纯 HA 客户也应能单装**。"融合"只做在体验层：语音加载项复用 run.sh 集成落盘模式自动安装 huijian_ai；网关 Web UI 增加"语音助手"跳转卡片；同仓兄弟目录 `huijian_voice/` + CI 矩阵双镜像 + 同条 Release 覆盖。
> 受影响小节：§1 表（模式 A 列作废）、§3（仲裁简化为"卫星常开"）、§5-3（固件仲裁项取消）、§8-M4（模式 A 承接删除）、§9 风险（云对账/迁移两项删除）、§10-Q2（已答：同仓独立加载项）。

---

## 1. 定案：双模式产品形态，**模式 B（HA 原生卫星）为主推**

| | 模式 A（现网/兼容） | **模式 B（新品主推，本方案主线）** |
|---|---|---|
| 音频链路 | 固件 ↔ 小智WS ↔ 服务器（服务器管会话/VAD） | 固件 ↔ **ESPHome API 6053（PCM上行/WAV下行）** ↔ HA assist_pipeline ↔ 加载项 |
| 意图执行 | 服务器 → MCP 反拨 → HA | 加载项 → **`POST /api/intent/handle`** → huijian_ai 14 意图（同源语义） |
| 固件 | 零改动 | **需移植 voice_assistant 组件**（§5） |
| 加载项工作量 | 需完整小智设备会话服务器（hello 协商/abort/连续对话/barge-in 状态机） | **三类消息的端点服务器**（消费端源码在手，协议=被消费语义，自写自接无外部依赖）——缩水约一半 |
| HA 增值 | 无 | 免费获得：多管线/按房间 select、**连续对话 continue_conversation**、定时器 TIMERS、宣告可起会话、唤醒词管理（含 tflite 下载通道）、VAD 调参、bcast/媒体既有通道（ffmpeg_proxy、ANNOUNCEMENT） |
| 存量客户 | 保持不动（不迁移原则） | 未来可选切换 |

**为什么 B 可行且更优**：集成侧卫星+assist 两条腿代码完整（【盘点】§2）；设备入驻/配网小程序链路现成；加载项只需实现
`stt/tts/llm` 三类消息端点（协议帧=hello、listen start/binary/stop→stt、tts detect→opus+stop、detect→text 聚合），
**消费端就在我们手里的源码——不存在未知协议**（v3 时代"抓包实锤"风险解除）。模式 A 的完整音频服务器降为 **P2 兼容件**（M4 后视需要）。

## 2. 模式 B 全链路图（一回合）

```
[待机] 设备本地唤醒"你好小智"(WakeNet9s, 固件现成)
  ▼ A 固件: wake → assist_satellite RUN START (feature: USE_WAKE_WORD)
  ▼ B mic: 裸 PCM s16le 16k/mono 流 (API_AUDIO 通道, 6053) → HA pipeline
  ▼ C HA: pipeline VAD 端点判定 (vad_sensitivity select 可调) → 整段音频
  ▼ D stt.huijian_asr: listen start/opus帧×N/stop → [加载项] Paraformer 流式(收帧即推, stop 出终稿)
        └ 热词纠错表(内导/内岛→内倒、统灯→筒灯、汇江→慧尖) 在此后应用；云STT可选(故障回落本地)
  ▼ E conversation.huijian_agent → [加载项 llm端点] 收 {"listen","detect",text}
        NLU 级联(全部在加载项内, <50ms 级):
          T0 正则+守卫(_ACTION_PATTERNS 全量窗控词) → T1 TextCNN14类(阈值按类) 
          → 场景触发(HassListVoiceScenes 60s缓存+trigger_index 对账)
          → 查询族("客厅多少度"→huijianGetLiveContext/states 读回播报) 【M1 新增】
          → 未命中且 LLM 已配 → 真LLM(15工具 function-call, OpenAI兼容)
          → 未命中且无 LLM → 固定兜底话术
        命中执行: POST /api/intent/handle {intent, data} (supervisor token)
          → huijian_ai 三级匹配/域名别名/窗户自动路由/auto补窗 现成容错
        回读状态生成真话术("已把书房窗开到50%")
  ▼ F 应答: {"type":"text",state:"sentence_end"}×N + end → ChatLog → TTS 分句
  ▼ G tts.huijian_speech: tts detect → [加载项] Kokoro(sid45 默认,分句) → 22.05k→16k→**opus60ms帧**+stop
  ▼ H HA→设备: WAV16k 512样本块流 (节奏=时长×0.9) + TTS_STREAM 事件；
        连续对话窗口内 B-C 循环(免唤醒)；窗口外回待机；abort(新唤醒/按键)→ 加载项清该会话 TTS 队列
[旁路] 每回合 utterance/执行结果 → HA 事件(改造点⑤ binary_sensor/事件) 供自动化与排障
```

**延迟预算（4C8G，本地档）**：C VAD 尾点 ~600ms → D 整句识别 ~300-700ms（3s 语音，流式吃帧已消化大半）→ E <50ms
→ 执行+回读 ~150ms → G 首句合成 ~300ms → H 起播 ~100ms。**说完→开口 p50≈1.3s / p90≈2.0s**（比模式 A 多一跳 HA，
换原生管线全家族增值，可接受；v3 的"快通道 <1s"目标在 LAN 档放宽到 p50 1.3s）。

## 3. 新增加载项 `huijian_voice`（语音服务器）职责清单

```
huijian_voice/
├── config.yaml            # host_network; ports: 8000(stt/tts/llm WS), 8001(管理UI经nginx), (P2模式A预留); 
│                          # discovery: zeroconf 广播 _huijian-voice._tcp (TXT: version, pairing); ingress; 
│                          # homeassistant_api: true(SUPERVISOR_TOKEN 代理复用慧尖网关成熟 nginx 模式)
├── core/
│   ├── ws_endpoints.py    # 三类消息端点(路径 /xiaozhi/v1/{stt|tts|llm}?token=***) —— 帧格式照【盘点】§2.2 实现
│   ├── session.py         # 每 WS 连接: 音频泵→识别器; tts 队列分句; 空闲180s自关(与消费端对齐)
│   ├── asr/sherpa_paraformer.py   # yy/asr 直挂, 流式收帧; 语言 zh-en
│   ├── asr/cloud_bailian.py 等    # M4; 接口面同构, 失败回落
│   ├── tts/kokoro.py      # yy/tts Kokoro ONNX; sid 表(默认45)/speed; 22.05→16k 重采样→opus
│   ├── nlu/fast_path_port.py      # 收编(修 P1 _HA_REQUEST_TIMEOUT→本方案改 intent REST; P2 纠错表缺失重建; P3 阈值按类)
│   ├── nlu/textcnn.py     # 14类模型随镜像 (~2MB)
│   ├── nlu/query_zone.py  # 查询族: states 缓存→读数话术
│   ├── agent/llm_optional.py      # OpenAI兼容 + 15工具 schema(照抄 huijian_ai 注册面) + 确认策略
│   ├── ha_bridge.py       # /api/intent/handle 客户端; /api/states 缓存(WS订阅); supervisor token
│   └── settings.py        # settings.json 单一事实源(v3 §4.2 结构不变, provider 键对齐集成端点)
├── www/                   # 设置页: 识别/理解(含测试框:输入文本→显示命中链路与intent JSON)/播报(8+音色试听)
│                          # 配对页: 设备入驻二维码(模式B: 引导 HA 发现卡片; 模式A保留: CMD12 URL 下发)
├── models.lock.json       # paraformer/kokoro 模型清单+sha256; 首启 modelscope→hf-mirror→手动导入(v3 定案不变)
└── run.sh / Dockerfile    # 慧尖网关同款模式复制
```
**默认即开**：装加载项 → mDNS 出现 → 集成发现卡片"配置"→ assist 条目端点自动填 `ws://<HA>:8000/xiaozhi/v1/*?token=<加载项生成>` →
管线选 `huijian 本地语音` → 完。零必填（用户要求②在 LAN 形态的最终实现）。

资源（4C8G 叠加慧尖网关共存场景）：加载项稳态 **≈2.0-2.2GB**（server 0.3 + paraformer 0.6 + kokoro 0.9 + NLU/misc 0.15）+
HA pipeline 侧增量(VAD/wav 处理) ~0.2GB + 网关加载项 0.3-0.5GB → **总盘 ≈3GB 内，余量健康**；线程帽 ASR2+TTS2；
省电档 `unload_when_idle_min`（卸载模型保 WS 网关，首句慢 ~2s）同 v3。

## 4. huijian_ai 集成改造清单（小补丁，仓库 huijian-yy 转正为主干后提交）

| # | 对应缺陷 | 改动 | 尺寸 |
|---|---|---|---|
| ① | D1 | `mcp_endpoint` 为空 → 跳过 mcp_transport 挂载（不再抛 AuthFailed） | 门禁几行 |
| ② | D3 | 新增 `_huijian-voice._tcp` zeroconf 发现→**assist 条目自动创建流**（端点/token 自 TXT+加载项 REST 取，回退=手动表单四键）；恢复 `zeroconf` 广播匹配键 | 1 个 flow step |
| ③ | D2 | 9 视图 `requires_auth=True` + 管理页带 token（M1 内做，公网暴露前硬门槛） | 小 |
| ④ | D5 | play_voice_text 引擎优先级：`tts.huijian_speech`(本地加载项) > edge-tts > 报错 | 小 |
| ⑤ | D8/D4 | 语音服务器在线 binary_sensor + 补 HassUpdateVoiceScene（intent 化 REST PUT 已有的 update_scene） | 小 |
| ⑥ | D7/D6 | mcp_server 惰性 import；custom_llm_api 清理（其 412 行工具定义**转正为加载项 llm 档的 function schema 蓝本**，注解 NameError 顺手修 D20） | 小 |
| ⑦ | D14-D19 | 语义缺陷包：number 增量叠加、automation `last_triggered` 回写、REST PUT 走 intent 同链、ControlWindow 全 targets 循环、**binary_sensor 触发支持（on/off→1/0）**——最后这条是产品级："检测到人体移动就开窗"现在语音建得成、永不触发 | 中（含回归测试） |

> 话术端注意（加载项生成总结句时）：**锁语义反转**（turn_on=上锁/turn_off=解锁）；泛称窗=全窗逐个按 0.5s 间隔；
> 温度/位置步进有就近对齐与步长违规文案；场景动作白名单 6 intent、单动作 30s 超时、全成功才 success。
> 好消息（二轮深读实锤）：HA core `intent.async_handle` **原样返回 handler 裸 dict**——加载项执行器拿
> `{success, control_targets/message/error}` 结构化结果直接拼真话术，无文本解析负担；`/api/intent/handle`
> 长时令牌即唯一对外入口（【盘点】§3.1）。

## 5. 固件改造清单（0513gujian 仓库，模式 B 的硬件侧；与 xiaozhi 路径共存）

**目标：设备同时具备"ESPHome Assist Satellite 人格"，保留现 xiaozhi WS 人格，运行时仲裁。**

1. **移植上游 `voice_assistant` 组件**（MIT，ESPHome 框架 components/voice_assistant）到内嵌 components/esphome/
   ——实锤缺口：当前捆绑组件清单无此目录，但 `api_connection.cpp:1266-1272` 的协议挂点已在（含
   `global_voice_assistant` 空指针守护），说明 API 层已按带 voice_assistant 的上游版本编过——M0 先核实真实编译面）。
   需要的 feature flags 声明：`API_AUDIO|SPEAKER|TIMERS|ANNOUNCE|START_CONVERSATION|USE_WAKE_WORD`。
2. **音频管线接线**：慧尖 AudioService(I2S 输入已有 AEC/降噪链) → 唤醒后 tap 出 16k PCM → voice_assistant
   ring buffer → API `send_voice_assistant_audio` 反向(HA 下行收包)→ 现 Opus 解码器旁路加 **WAV/PCM 直解** → 输出混音。
   xiaozhi 路径的 opus 编解码器全部复用（STT 上行同样吃 opus——但模式 B 上行是裸 PCM，CPU 增量小）。
3. **仲裁开关**（核心新增）：`ha_satellite_enabled` NVS 标志（BLE CMD30/CMD12 扩展位或加载项经集成下发）：
   - 卫星模式：wake → 仅走 VoiceAssistant 事件链（`wake_detected`→HA），xiaozhi WS 静默（保活不传音频）；
   - xiaozhi 模式：现状不变（含屏显/音乐联动）。
   - 防双抢麦：HA API `subscribe_voice_assistant` 活跃期挂起 xiaozhi listen；反之亦然。
4. **播报兼容**：media_player ANNOUNCEMENT + `play_voice_text`（HA 侧行为变成本地 TTS）+ `/api/esphome/wake_words`
   tflite 拉取通道（自定义唤醒词 P2，现 WakeNet9s"你好小智"保留为默认）。
5. **定时器实体面**：TIMERS flag 的固件端实现（upstream 有 C++ 参考实现，简单 ring 显示 + 到点响铃）——首版可先报 0 支持。
6. 屏显/灯光律动在卫星模式下由 HA 事件驱动（`huijian_voice_utterance`/pipeline events）——P2 美化项。

工作量估算：1-2 名固件工程师 × 2-3 周（含真机联调），大头是混音/回路与仲裁的稳定性打磨。

## 6. 小程序（改动最小化确认）
- 配网+设备入驻（CMD10/CMD20→`/api/huijian-ai/setup/qrcode` 回填 device 条目）**现流程原样可用**（模式 B 不依赖 CMD12 的 xiaozhi URL；写了也无害——固件仲裁标志决定走哪条）。
- assist 服务条目：主路径=HA 内发现卡片（§3 零配置）；**辅路径保留**：小程序在"连接慧尖语音服务器"页 POST config_type=assist setup_data（端点来自加载项 mDNS 探测）——给不想开 HA UI 的用户。改动：LAN 分支绕云函数（v3 定案），新增页面一张。

## 7. 意图覆盖与"控制 HA 全部设备"完备性论证
- **默认本地档**（T0+T1+场景+查询族，无 LLM）：高频家居控制全覆盖（14 域×区域/名称/位置/色温/模式/风速/湿度，
  窗控含内倒/暂停/百分比，场景四类操作+传感器自动化四类），误执行由三级匹配+宁漏判话术守住。
- **扩展档**：LLM（可选）补长尾口语与多步编排（先查后创流程现成于意图描述文案）；Klar（可选）补更宽泛语法层——
  v3 定位不变，默认关。
- **评测**：`意图数据集/慧尖HA集成完整数据集.jsonl`(436) 改造成回归评测集（M1 门禁：控制类≥95%、查询类≥90%、误执行 0；
  模式 B 真机端到端跑，不拿客户当测试）。

## 8. 里程碑（v4 重排）

- **M0′ 三向拉通（1.5 周， spike）**
  ① 加载项骨架跑通三端点（假 echo 服务：stt 固定回文本/tts 播蜂鸣/llm 回固定句），**与真 huijian_ai 集成（测试 HA）联调**
     ——消费端源码即协议权威，联调即验收；
  ② 固件原型：voice_assistant 移植 + PCM 上行 + WAV 下行播通（1 台 S3）；
  ③ fast_path 收编三修（P1 端点层换 /api/intent/handle；P2 纠错表按文档重建；P3 阈值按类）+ 单测。
  **M0′ 验收金标准：真机说"打开客厅灯" → 灯亮 → "已打开客厅灯"落地本地档全链路，p90 ≤2s。**
- **M1′ 产品化 MVP**：模型下载体系 + 设置 UI + Paraformer/Kokoro 实挂 + 436 评测集门禁 + 查询族 + 快照型建场景
  （"把现在客厅保存为观影模式"→live_context→组 Turn* actions→HassCreateVoiceScene，确定性可做无需 LLM）
  + 集成补丁①②③⑤ + 固件仲裁稳定化 + 场景触发词 60s 缓存对账。
- **M2′ LLM 档 + 多房间**：llm_optional（custom_llm_api 转正 schema）+ 确认策略 + 多卫星（assist 条目 per 实例；
  卫星归属房间走管线）+ 连续对话/打断打磨。
- **M3′ 配网闭环**：小程序 LAN 分支 + 加载项配对页 + 出厂预置话术包 + 客户文档。
- **M4′ 云与兼容**：云 STT/TTS provider（百炼/火山/OpenAI 兼容，含慧尖 SFT 预置模板）+ **模式 A 设备直连音频服务器**
  （现网云到期客户回迁承接体）+ Klar 评估转正。

## 9. 风险登记（v4 增删）

| 风险 | 级 | 缓解 |
|---|---|---|
| 固件 voice_assistant 移植与慧尖自有音频链冲突（AEC/混音/barge-in） | 高 | M0′ 只做协议贯通，稳定性留 2-3 周缓冲；最坏降级路=模式 A 加载项版（完整音频服务器，v3 方案，仍可控） |
| llm 端点 `type=text/sentence_end` 聚合语义与 ChatLog delta 适配踩坑 | 中 | 消费端代码在手，M0′ 联调即暴露；协议快照落盘 `docs/protocol/` |
| 客户现网 huijian_ai 版本 > 本仓库（版本源漂移 D13） | 中 | 上架前从任一在网客户 HA 拉 live 版对账（我方测试 HA 先行，不动客户）；转正主干+CHANGELOG |
| D2 无鉴权洞在发现流推广后被扫 | 高 | 补丁③进 M1 硬性门禁；公网形态（若有）延至补丁③后 |
| edge-tts 移除改变现网播报行为 | 低 | 补丁④仅改优先级，联网可用 edge-tts 兜底不变 |
| 4C8G 三件共存（网关+语音+HA pipeline）内存/IO 尖峰（冷启动并发） | 中 | M0′ 真实 HA 压测曲线；冷启动错峰（模型下载限远端+加载项 startup 事件延后） |
| Paraformer 整句 batch 通道下长句延迟 | 低 | 收帧即喂流式模型（消费端逐帧发，我们不必等 stop） |
| PAT 泄露（D12） | 急 | 用户吊销重发（今天） |

## 10. 待用户确认（v4.1 后仅剩 3 件，均不阻塞 M0′）
1. **固件改造用哪台设备先行**：在库 S3 开发板 or 特定量产批次（涉及 PSRAM/回音硬件差异）？
2. ~~仓库关系~~ **已定（v4.1）**：同仓 `huijian_voice/` 独立加载项独立镜像，集成补丁提交 huijian-yy（HACS 独立分发）。
3. 现网若有任何一台测试/演示 HA（非客户），给我访问方式做一次 live 版本对账与 llm 通道观测（我方实证规矩，只读）。
4. 文档归档口径：v1/v2/v3 保留为演进档案，v4+盘点两文为现行——收到确认后我把两文合并重排为正式《产品设计说明书》v1.0。

---
### 与旧版方案差异速览
- v3「自研完整小智音频服务器」→ v4「**模式 B 三端点服务**（工作量减半）+ 模式 A 降 P2」
- v3「MCP→REST 双通道取舍」→ v4「MCP 仅模式 A 遗产保留；模式 B 全程 /api/intent/handle」
- v3「固件零改动」→ v4「**固件移植 voice_assistant + 双模仲裁**（用户授权）」
- v3「新建 huijian_voice 集成」→ v4「**复用 huijian_ai 集成 + 6 小补丁**」
- 协议未知风险：v3 需抓包实锤 → v4 消费端源码在手，**风险解除**
