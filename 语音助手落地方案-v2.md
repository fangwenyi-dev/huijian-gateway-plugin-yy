# 慧尖语音助手 · 落地方案 v2（2026-09-06）

> 需求基线（用户 2026-09-06 定调）：
> ① STT/TTS/NLU **默认全部内置本地模型**（STT/TTS 可配置切换云端）；LLM 用户自选，**默认不需要**；
> ② 安装即默认配置可用；用户在集成/插件中可把默认改成远程；
> ③ 小程序职责 = **给 ESP32 配网 + 连到 HA**（不承载对话链路）；
> ④ 全流程梳理：STT → NLU（快速通道；LLM 在其后）→ 执行 → TTS。

---

## 0. 与 v1 研究稿的关键差异（及原因）

| 决策点 | v1 | v2（本方案） | 原因 |
|---|---|---|---|
| 语音服务器 | 打包 xiaozhi-esp32-server 单模块 | **自研轻量服务器**（实现小智 WS 协议子集） | 上游管线 LLM/Memory 必经，无法"默认无 LLM"仍处处得体回应；且固件是我们自家 fork（0513gujian），协议本就双向可控，"跟上游兼容"不构成理由。上游资产不浪费：fast_path 规则/TextCNN/53 条纠错表**源码收编**为自己的 NLU 引擎 |
| 音箱在 HA 的呈现 | 自动创建 ESPHome 条目（6053） | **内嵌 `huijian_voice` 集成**直接注册设备/实体；ESPHome 直连降级为 P2 可选 | ESPHome 配置流对 Noise PSK 的注入路径需专门实验（风险项）；自家集成走文件/localhost HTTP IPC，模式与窗控集成同构，零新风险 |
| 配网后设备如何"找到"服务器 | mDNS 广播 + 兜底 | 不变：小程序 LAN 化发现（**二维码首选**）→ **CMD12 写 NVS**（固件零改动） | 微信 mDNS 不透传 TXT/iOS 不支持是实锤；二维码带完整 URL+token，全平台可用 |
| 云端定位 | LLM 默认云 | **全部默认本地**；STT/TTS/LLM 三处独立可切云 | 即用户定调① |

其余结论（独立姊妹加载项、模型选型 Paraformer+Kokoro、端口规划、资源预算）沿用 v1，不再重复论证。

---

## 1. 产品形态与系统架构

```
┌──────────────────────────── HA 主机 4C8G（HAOS）────────────────────────────┐
│                                                                            │
│  [慧尖网关加载项(现有)]  Mosquitto:2022 + 窗控集成 + Web:8099                │
│                                                                            │
│  [慧尖语音加载项 huijian_voice(新)] host_network                            │
│   ├ voice_server (Python asyncio)                                          │
│   │   :8000  小智WS（Opus音频+JSON控制，token 鉴权）  ◀──── ESP32 音箱×N     │
│   │   :8003  OTA/配对/管理HTTP（token 鉴权）          ◀──── 小程序配网写址    │
│   │   :8098  设置/测试Web UI（nginx 仅限本机，走 ingress）                   │
│   │   管线: VAD→STT→NLU→[LLM?]→执行→TTS（§2 全梳理）                        │
│   ├ models(/data): Paraformer int8 · SileroVAD · Kokoro · TextCNN ONNX     │
│   └ ha_bridge: HA REST(经 homeassistant_api/SUPERVISOR_TOKEN, 与现有       │
│        /api/ha/ 同模式) + WS 事件订阅（实体图/场景图缓存）                    │
│                                                                            │
│  [内嵌 custom_components/huijian_voice(新)]                                 │
│   · 音箱设备表（在线/房间/名称，读 /homeassistant/huijian_voice/ 状态文件）   │
│   · 服务 huijian_voice.announce(音箱,文本) → localhost:8003 → speaker_tts    │
│   · 事件 huijian_voice_utterance(设备,识别文本,意图,结果) → HA 自动化可用     │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
     ▲ 配网(一次性)：小程序 BLE ── WiFi+服务器URL+token 写 NVS（CMD10/12 现成协议）
     │ 语音(实时)：唤醒"你好小智"(设备本地 WakeNet9s) ── LAN 内闭环，默认零出网
```

**默认即离线可用**：断外网时"控制设备/激活场景/保存场景"全链路照常（本地 STT/NLU/TTS）；仅云选项和（可选的）LLM 失效并有固定话术告知。

---

## 2. 语音全流程梳理（需求④：逐环节、分支、时序、兜底）

### 2.1 主状态机（每音箱一个会话）

```
[待机] 设备本地唤醒词"你好小智"(不耗服务器资源)
  │ 设备: listen:start(mode=auto) + hello 已协商(Opus 16k/60ms 上行, 24k 下行)
  ▼
[A 拾音上行] Opus→PCM16k → ①VAD(Silero,本地) 边收边转写
  ▼
[B 整句判定] 尾静音 ~600ms(服务端 endpointing) 或设备 listen:stop → 定稿文本
  ▼
[C ②STT] 默认: sherpa 流式 Paraformer 中英 int8(本地)
  │        → 后处理: 53条热词纠错表(统灯→筒灯/内导·内岛·内到→内倒/汇江→慧尖)
  │        → 可选云: 百炼Paraformer·OpenAI兼容whisper(配置见§4，失败自动回落本地)
  │  空文本/纯噪声 → 轻"嗯?"提示音或静默回[待机]
  ▼
[D ③NLU]（nlu.enabled=true 默认）快速通道，全部本地、目标 <50ms：
  │  L1 规则: 动作×区域前缀×实体名匹配(内存HA实体图; 中文数字/百分比/亮暗归一)
  │  L2 TextCNN ONNX 15类意图+置信阈值(场景0.5/其他0.7/OOS0.85，沿用实测定参)
  │  L3 场景触发词表(HA scene 实体, WS事件增量, 免60s轮询)
  │  ├─命中→ 直接执行意图(见E)，"宁漏判不误执行"
  │  ├─歧义(多实体同名)→ 固定话术追问一次("是客厅的哪个灯?")，结合上下文消歧一次
  │  └─MISS/OOS → ↓
  ▼
[E ④LLM]（llm.enabled=false 默认不装配）
  │  已配置(OpenAI兼容 base_url+key+model)时:
  │    tools=[查实体/调服务/场景·激活·快照建·编辑/自动化创建/天气可选]
  │    承接: 复杂指令、编排型建场景、闲聊、多轮(每设备内存内滚动历史,默认10轮)
  │    → 产出同样是"执行动作+回复文本"两件套
  │  未配置时: 固定兜底话术(本地TTS)——
  │    "这句我还不太会。可以说：打开客厅灯、窗户开到50%、我出门了；
  │     或在语音助手设置里接入大模型解锁更多能力。"
  ▼
[F 执行] 意图→HA 服务调用(REST, supervisor token)：
  │   light.turn_on/cover.set_position(开窗到50%)/climate/scene.turn_on/
  │   config/scene/config/{id} 写(语音建场景,§2.3)/automation 写(LLM编排)
  │   执行后回读状态生成总结句(真话术:"已把书房窗开到50%"，失败也说失败原因)
  ▼
[G ⑤TTS] 默认: sherpa+Kokoro int8 本地(中文音色 sid 可选,语速可调)
  │   分句流式: 逐句 sentence_start, **首句先行合成先行下发**(不等全文)
  │   → Opus 24k 下行: tts:start → 音频帧 → tts:stop
  │   可选云: 百炼CosyVoice/OpenAI兼容TTS(失败回落本地)
  ▼
[H 回合] 连续对话窗口(设备CMD30设置联动): 窗口内免唤醒直接说→回B
  │   静默超时→listen:stop→回[待机];  打断(barge-in): 播放中再唤醒
  │   →设备发 abort→服务器取消TTS队列→回A
  └─ 全程每句产 huijian_voice_utterance 事件(供HA自动化) + Web UI 语音日志
```

### 2.2 环节组合行为矩阵（需求①"NLU/LLM 的三种排布"）

| nlu.enabled | llm.enabled | 行为 | 适用 |
|---|---|---|---|
| ✅(默认) | ❌(默认) | 快通道命中即执行；MISS→固定兜底话术 | 零配置开箱、全离线 |
| ✅ | ✅ | 命中直执(<1s)；MISS/低置信→LLM(函数调用+闲聊) | **推荐进阶**：快且省 token |
| ❌ | ✅ | 每句走 LLM(2-6s,吃 token,但理解最泛) | 极客模式 |
| ❌ | ❌ | **禁止**（设置界面互斥校验，保存即提示） | — |

STT/TTS 同理独立两态（本地/云），共 8 种组合互相正交，任一云端调用失败**自动回落本地并语音致歉一次**（不中断服务）。

### 2.3 语音建场景（默认本地即可完成的部分，不需要 LLM）

1. "把现在客厅的样子保存为观影模式" → NLU `SaveScene(area=客厅,name=观影模式)`；
2. 执行器：GET `/api/states` → 按区域过滤可控域(light/cover/climate/fan/media_player)→ 组装 entities 快照；
3. **POST `/api/config/scene/config/{id}`**（UI/storage 场景，重启持久；ha-mcp 在用。⚠️ 勿用 `scene.create` 服务——上游 issue #133912 实锤其临时态重启即丢）→ `scene.reload` → 立即可被"打开观影模式"激活；
4. 上限/去重/覆盖确认走固定话术（"已保存观影模式，包含客厅主灯、窗帘、氛围灯 5 个设备"）；
5. **编排型**（"每天六点拉窗帘"）→ 属 LLM 函数 `create_automation`（写 `/api/config/automation/config/{id}`），未配 LLM 时兜底话术明确说明。
6. 🔬 M1 真栈验收项：Supervisor token 对 config REST 写权限是否 admin 级（HA config API 要求管理员；不通则改走"自建慧尖场景库实体"兜底，方案不变、落点不同）。

### 2.4 端到端时序预算（4C8G，默认全本地）

| 环节 | 预算 | 依据 |
|---|---|---|
| 唤醒音→服务器就绪 | 已有 WS 长连接 ≈0 | hello 会话复用 |
| 整句判定（尾静音窗） | ~600ms | endpoint 规则 |
| STT 增量转写 | 与说话并行，定稿后 <100ms | sherpa 流式、int8、2线程 |
| NLU | <50ms | 正则+TextCNN ONNX |
| HA 调用+回读 | <150ms | 本机 REST |
| TTS 首句出声（合成 RTF） | 300-500ms | Kokoro q8 CPU（M0 实测） |
| **说完→开始回应 合计** | **≈1.2-1.7s** | 验收线 p90 ≤2s（快通道）；LLM 路径另计 2-6s |

---

## 3. 组件选型与默认/云对照（需求①）

| 环节 | 默认（内置本地） | 可配置远程 | 凭证 | 来源 |
|---|---|---|---|---|
| VAD | Silero ONNX | — | — | 随镜像 |
| STT | sherpa **Paraformer 流式中英 int8**（226MB） | 百炼(DashScope) Paraformer-realtime / OpenAI 兼容 whisper / （可扩讯飞） | api_key(password 字段) | ✅ yy/asr 已下载同款 |
| 热词纠正 | 53 条表 + **用户自定义表可增删**（Web UI） | — | — | 云服务器现成资产收编 |
| NLU | 规则+**TextCNN 15类 ONNX**+场景表（nlu.enabled 可关） | 无云端需求（确定性） | — | fast_path 源码收编（🔬待归拢，§10-3） |
| LLM | **无（默认关闭）** | 任意 OpenAI 兼容端点（DeepSeek/百炼/Ollama 本地加载项…） | base_url+key+model | 纯配置 |
| TTS | sherpa **Kokoro-82M**（quantized ~92MB，中文音色 12 个可选，语速可调） | 百炼 CosyVoice / OpenAI 兼容 TTS | api_key | ✅ yy/tts 已下载同款 |
| 执行 | HA REST（本机，supervisor token） | — | — | 现有 /api/ha/ 模式复制 |

不进加载项（存档结论）：CosyVoice3-0.5B(19GB/GPU)、nemotron(5.7GB/NeMo)；MOSS-TTS-Nano 与 Qwen3-TTS 进**观察池**（若 M0 盲听 Kokoro 中文不满意再评估替换，接口已 provider 化）。

---

## 4. 配置体系（需求②：默认即用 + 事后可改）

### 4.1 单一事实源与三个入口
- **`/data/settings.json` 唯一生效配置**；首次启动自动生成全默认值 → **装完即用，零必填**。
- 入口 1（推荐）：**加载项 Web UI「语音助手」页**——分组表单（识别/理解/大模型/播报/设备），带**文本模拟测试框**（输入一句中文→展示 STT 纠错→NLU 命中链路→将执行的动作，不真执行），改云端项前有"测试连通"按钮。
- 入口 2：Supervisor「配置」options——只放少量高级字段（端口偏移、token 重置、省电模式、模型管理），schema 校验。
- 入口 3：集成侧（`huijian_voice` 选项流）——**按设备**配置：房间绑定、默认应答音色、连续对话开关（透传设备 CMD30）。
- 生效规则：Provider 级热应用（下一回合生效，不重启不断设备连接）；端口/token 级改动才重启服务进程。

### 4.2 默认值表（settings.json）
```jsonc
{
  "stt":  {"provider":"local_paraformer","language":"zh-CN",
            "cloud": null /* {"provider":"bailian","api_key":"***","model":"…"} */},
  "corrections_extra": [],                       // 用户自定义热词 追加到53条基础表
  "nlu":  {"enabled":true,"fuzzy_ambiguity":"ask_once","max_area_scan":200},
  "llm":  {"enabled":false,
            "openai_base_url":"", "api_key":"", "model":"",
            "history_rounds":10, "allow_scene_write":true, "allow_automation_write":false},
  "tts":  {"provider":"local_kokoro","voice":"zm_yunyang","speed":1.0,
            "cloud": null},
  "dialog":{"fallback_text":"这句我还不太会…（§2.1-E 文案，可编辑）",
             "wake_reply":null /* 设备侧应答,预留 */,"continuous_default":true},
  "power":{"unload_when_idle_min":0 /* 0=常驻; >N分钟空闲卸载模型省~1.5G，冷启动+1.5s */},
  "security":{"pairing_token":"<随机生成>", "ws_token":"<随机生成>"}
}
```
（`allow_automation_write` 默认关：LLM 写自动化必须经 Web UI/语音二次确认。）

### 4.3 加载项 config.yaml options（Supervisor 面，仅高级）
```yaml
options:
  ws_port: 8000        # 设备侧约定值；改动仅影响新配网设备(URL整体下发,无协商问题)
  ota_port: 8003
  ui_port: 8098
  pairing_token: ""    # 空=自动生成, Web UI 二维码即含它
  model_auto_download: true
schema: {…类型校验略…}
arch: [amd64, aarch64]   # host_network: true, homeassistant_api: true, map: homeassistant_config:rw
ingress: true / panel_title: 慧尖语音 / watchdog: tcp://[HOST]:8003
```

---

## 5. 配网与入驻流程（需求③：小程序 = WiFi + 连到 HA）

### 5.1 时序（全部复用现成协议，固件零改动）
```
小程序「语音助手→添加设备」：
 1) BLE 扫描(现成：名字含 huijian/esp32/xiaozhi) → 连接 → token 握手(CMD100)
 2) CMD2/3 扫描列 WiFi → CMD10 写 SSID/PWD                ←—— 配网职责到此为止
 3) 取得"慧尖语音服务器"地址，三来源(优先级序)：
    a) 扫码：语音加载项 Web UI「配对二维码」= huijian-voice://ota=http://<ip>:8003?token=***
       （ha-connect 现成扫一扫，任何机型可用）
    b) 局域网发现：wx.startLocalServiceDiscovery('_huijian-voice._tcp')→取 IP→端口按约定8003
       （微信不透 TXT/iOS 不支持 → 仅 Android 快捷路径）
    c) 手输 IP(:端口)（全平台兜底）
 4) CMD12 写 [wsUrl=ws://<ip>:8000/xiaozhi/v1/, wsToken, otaUrl=http://<ip>:8003/…, mqttInfo=空]
    → 设备 esp_restart → 连 OTA → 连 WS hello(Device-Id=MAC)
 5) 加载项：登记设备(MAC)→ Web UI 出现"新音箱待命名"→ 用户命名+选房间
    → 内嵌集成建 device_registry 设备+实体 → HA 设备页可见 ✅「已连接到 HA」
 6) (可选) 若带 Noise PSK(CMD20) → 追加 aioesphomeapi 直连会话 → 解锁 6053 上的
    volume/mute/play_voice_text 原生实体（P2，不作为 MVP 依赖）
```
**注意**：局域网模式下小程序**不再需要微信云函数取 URL**（现公网流程 `getServerConfig` 失败硬中断——LAN 分支绕过之，这正是 §8-M3 的主要小程序改动）。

### 5.2 设备生命周期
| 状态 | 判定 | 呈现 |
|---|---|---|
| 未入驻 | MAC 从未连过 | 仅 Web UI 配对页 |
| 在线 | WS 会话活跃；hello 心跳 | 实体 available |
| 离线 | 会话断开 + 静默 90s（对齐现网 GATEWAY_TIMEOUT 定式收紧） | unavailable + 可选通知 |
| 固件可更新 | OTA 应答带 firmware 段（我们自签发布） | 详情页提示 |
| 重配/换网 | 用户再次 CMD12 覆写即迁移（含从公网云迁回 LAN，存量设备无缝） | — |

改名同步：沿用现链 `POST /api/huijian-ai/update/speakname`——**该端点由语音加载项 :8003 实现**（token 校验后更新设备名并同步进 HA device_registry）。

---

## 6. 工程落点（新增目录与 CI）

```
huijian-gateway-plugin/
├── huijian_mqtt_broker/            # 现有，不动
├── huijian_voice/                  # ★ 新加载项（独立版本 1.0.x）
│   ├── config.yaml  Dockerfile  run.sh  requirements.txt
│   ├── voice_server/
│   │   ├── session.py        # §2.1 回合状态机（核心）
│   │   ├── ws_channel.py     # 小智WS子集：hello/listen/tts/stt/abort + 二进制Opus帧
│   │   │                     #   （含慧尖扩展 speaker_tts/speaker_order/front_speaker_order、
│   │   │                     #    tts play_start/play_stop/listen_start —— 契约=固件 docs/websocket.md）
│   │   ├── ota_http.py       # :8003 OTA应答(activation 省略=免绑)/配对token/管理API(本机)
│   │   ├── audio.py          # opus 编解码 + 重采样(16k↔24k) + 环形缓冲
│   │   ├── providers/        # stt_local/stt_cloud · tts_local/tts_cloud · llm_openai · vad
│   │   │                     #   统一 provider 接口：async transthesize/stream/test_connect + 回落包装
│   │   ├── nlu/              # rules.py + textcnn.py + corrections.py + scenes.py + resolver.py
│   │   ├── ha_bridge.py      # 实体图WS订阅缓存 · REST执行器 · 状态回读 · scene/automation config写
│   │   ├── tools_llm.py      # function schemas + 计划执行(带确认门)
│   │   ├── devices.py        # MAC↔名↔房间↔状态；/homeassistant/huijian_voice/devices.json 投影
│   │   └── settings.py       # §4 加载/校验/热应用
│   ├── www/                    # ingress UI（沿用三文件化模式）：设备/二维码/测试/设置/日志
│   ├── models.lock.json        # 文件名+sha256+多源URL(modelscope主→hf-mirror备)+手动导入指引路径
│   └── tests/                  # §8 测试资产
├── huijian_voice/custom_components/huijian_voice/   # 内嵌集成(随加载项落盘,复用 install 模式)
│   ├── config_flow.py manifest.py entity.py services.yaml  # announce/事件/选项流(按设备)
├── repository.yaml             # 追加 huijian_voice
└── .github/workflows/          # ci.yaml 扩矩阵：{arch}-huijian-voice 双架构镜像 + pytest 双套
```
镜像策略：base + apk(python3/nginx/opus-libs) + pip(aiohttp/sherpa-onnx/onnxruntime/numpy)；**模型不进镜像**（首启多源下载→/data/models；sha256 校验；失败给手动导入路径，UI 显示进度）——镜像本体控制 ~350MB 内，家宽 ghcr 拉取体验可接受（42MB→3-4min 的历史基线折算 ~30min 太糟，配合 CI warm + Web UI 进度 + modelscope 国内源下载模型反而快）。

---

## 7. 资源与安全基线

**RAM**（默认全本地，LLM 关）：server+管线 0.35G + Paraformer 0.6G + Kokoro 0.9G + VAD/TextCNN/杂 0.15G ≈ **2.0-2.4GB**（比 v1 更省，无 LLM 常驻）；`power.unload_when_idle_min` 可再降。config.yaml 不做内存硬限制但 run.sh 上报水位到 UI；HA 侧 OOM 风险由"语音加载项可独立重启"隔离。
**CPU**：ASR 2 线程 + TTS 2 线程封顶（options 可调 1/2），空闲时管线近 0；N 台音箱并发说话才叠加（家庭 ≤2 台无压力）。
**安全**：① WS/OTA 全部 token 鉴权（配对 token 由加载项生成，二维码/手动分发，可一键重置）；② 8098 管理面 nginx 仅 127.0.0.1+172.30.32.x（复制现有 ingress 定式）；③ api_key 用 Supervisor password 型字段脱敏存储；④ 已知残余风险：BLE 无鉴权（任何近距离者可改写服务器地址/触发 OTA URL）——MVP 接受（风险=指到别的服务器，且我们的 OTA 应答可不下发固件升级），固件帧签名列入后续版本；⑤ 隐私口径：默认零出网；启用云 STT/TTS/LLM 时明示"文本/音频将发送至您配置的云端点"。

---

## 8. 里程碑与验收（真栈实证，不拿客户当测试）

### M0 · 协议与延迟实锤（3-5 天，独立 spike，不进主仓）
- 交付：`spike-voice/`（Python aiohttp + sherpa-onnx python + silero + opus），一台 S3 真机经 `tools/cmd12_writer.py`(bleak 新写) 指向局域网；HA 真栈接 1 个测试灯/窗。
- 验收（全过才进 M1）：
  ① hello 握手/二进制帧版本(1/2/3)/采样率协商与固件实测对齐，产出《协议实测记录》对照 docs/websocket.md 勘误；
  ② "打开{测试灯}"本地链路 **说完→开灯→中文语音回应** 实测延迟表（p50/p90）；Kokoro 中文试听 5 句过产品线（不过→启动 MOSS-Nano 备选评估）；
  ③ barge-in/连续对话窗口/断线重连 三行为验证；④ 4C8G 真机 RAM/CPU 曲线（`docker stats` 采样 1h）；⑤ NVS 残留 `mqtt.endpoint` 干扰项处置（CMD12 mqttInfo 传空串=清除，需实测确认固件语义）。

### M1 · 加载项 MVP（本地闭环，2-3 周）
- 交付：§6 全目录（LLM 除外）：双架构镜像+CI、models 首启下载、Web UI（设备/二维码/文本模拟/设置/日志）、NLU 引擎收编（规则+TextCNN+纠错+场景表）、HA 桥（含 **2.3 快照建场景** 真栈验证）、内嵌集成（音箱设备+announce 服务+utterance 事件）。
- 验收：装→扫→配→控 全流程；**100 句家居指令集**（含慧尖话术：内倒/停/开到50%/我出门了…）执行正确率 ≥95%、零误执行；断外网全绿；升级/重启后设备与设置不丢；pytest 全绿入 CI（新增 lint 同款）。
- 测试资产：`tests/nlu_dataset_100.yaml`（新建，M0 语料扩充）、协议回归（hello/整句/回合断言）、heredoc 静态扫描等既有规范沿用。

### M2 · LLM 可选档 + 多音箱（1-2 周）
- 交付：openai 兼容客户端+function tools（实体/服务/场景 CRUD/自动化带确认）+按设备历史+房间作用域 prompt+歧义消解多轮。
- 验收：三档矩阵（§2.2）行为各 10 例；配/不配 LLM 切换热生效；"每天晚上六点拉窗帘留阳台灯"→确认→automation 出现且可编辑。

### M3 · 小程序局域网配网（1 周，含微信审核周期余量）
- 交付：「语音助手」向导加"连接我的慧尖 HA（局域网）"分支：扫码/列表/手输三来源 → CMD12 LAN 值；存量公网设备"迁移到局域网"入口；改名同步走新端点。
- 验收：三来源端到端各 3 轮；iOS（无 mDNS）全通；公网↔LAN 双向迁移无损；云函数零依赖路径。

### M4 · 云 STT/TTS 配置档 + 打磨（1-2 周）
- 百炼/OpenAI 兼容双云 STT/TTS provider + 回落 + 连通测试按钮；多房间/别名精调；aarch64 NAS 性能报告与推荐机型；《客户安装手册》（含"语音助手 30 秒上手"）。

（P2 展望：ESPHome 6053 原生实体、设备端 MCP（屏/音量类）、Klar 可选第二 NLU、唤醒词定制"你好慧尖"（固件+esp-sr 训练）、RagFlow 问答——仅配得上独立服务器。）

---

## 9. 风险登记（含 v1 存量）

| # | 风险 | 级 | 缓解 |
|---|---|---|---|
| 1 | 协议细节踩坑（fork 偏离上游：自定义消息/二进制版本/MQTT 优先级） | 高 | M0 真机 spike 前置；固件 docs/websocket.md+docs/mqtt-udp.md 为契约；实测记录归档 |
| 2 | Kokoro 中文音质/韵律不达标 | 中 | M0 试听门；MOSS-TTS-Nano ONNX 备选（provider 接口已留） |
| 3 | sherpa/CosyVoice 等上游 fast_path 资产不在仓（云服务器 docker-cp 状态） | 高 | **§10-3 前置动作**：从现网容器把 fast_path.py/TextCNN 模型/纠错表/provider 源码拉回归仓（否则 M1 无法收编） |
| 4 | config REST 写场景的权限/版本门槛 | 中 | M1 真栈实测；兜底自建场景实体（UI 可激活/自动化可引用，仅不在原生场景编辑器） |
| 5 | 设备粘滞：语音加载项挂=音箱离线（fork 不被云抢回，双刃剑） | 中 | watchdog 字段+崩溃自愈+UI 状态灯；HA 通知"语音服务离线"自动化模板随集成提供 |
| 6 | 首启模型下载在家宽失败 | 中 | modelscope 主源+多源重试+sha256+手动导入包（发 Release 资产）+离线安装指引 |
| 7 | 4 核竞争导致 RTF 恶化（NAS ARM 更弱） | 中 | 线程封顶+M0/M1 真机曲线+机型推荐；power 懒载档 |
| 8 | 小程序改动过审周期/真机差异（iOS 无 mDNS） | 低 | 扫码主路径天然跨平台；体验版先行 |
| 9 | BLE 写入无鉴权（改址/OTA 面） | 低-中 | MVP：OTA 端点不响应固件段即无远程刷入面；token 收敛服务面；帧签名列入固件 backlog |
| 10 | ESPHome 条目 Noise PSK 注入路径未验证 | 低（已出 MVP 关键路径） | P2 再实验，MVP 走内嵌集成 |

---

## 10. 需要拍板（收敛到 5 个）

1. **云 STT/TTS 首选厂商**：建议 **阿里百炼 DashScope**（你已有 dashscope 通道，Paraformer/CosyVoice 同源同文），OpenAI 兼容作通用第二档——同意即定。
2. **默认 TTS 音色**：Kokoro `zm_yunyang`（男）/`zf_xiaoxiao`（女）选一个出厂，其余 UI 可选——建议女声。
3. **云服务器现网资产归拢**（fast_path.py、TextCNN 模型与训练数据、53 纠错表、两个 sherpa provider）：给我现网容器访问路径或你手动导出一次——**M1 硬前置**。
4. **现网公网小智云与 LAN 方案的关系**：新客全 LAN 默认？存量公网客户"可选迁移"还是"到期切换"？（影响 M3 优先级，不影响 M0-M1。）
5. **首版意图覆盖面**：15 类 TextCNN 沿用（开关/位置/亮度/色温/温度/模式/风速/场景/OOS…）+慧尖窗控全词——够不够第一版？要不要加"查状态问答"（"客厅多少度"——本地规则即可答，建议加）。

> 本文件与《语音方案研究-2026-09-05.md》配套：v1 存调研证据（固件/文档/模型体检逐条 file:line），v2 为执行基线。确认后从 M0 spike 起跑，不动现有任何代码。
