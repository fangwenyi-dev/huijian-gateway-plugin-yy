# ═══════════════════════════════════════════════════════════════════
# 慧尖语音加载项 Python 依赖（运行时钉版；构建期三级 pip 镜像回退见 Dockerfile）
# 实证背景（2026-09 研究轮 + 本机 76 项测试全绿）：
#   · sherpa-onnx / onnxruntime / numpy 均无 musl wheel → 基础镜像必须 glibc
#     （hassio-addons/debian-base，网关加载项的 Alpine 模式不可照抄）；
#   · numpy 2.x + onnxruntime 1.29 + sherpa 1.13.7 同环境实测兼容（旧「锁 <2」
#     结论源于 ort<1.17 时代的 ABI 冲突，已随版本线失效）；
#   · sherpa-onnx wheel 覆盖 cp310–cp314，python 3.11–3.13 均安全，避开 3.14。
# ═══════════════════════════════════════════════════════════════════
aiohttp>=3.13,<4            # WS 服务器 + HA REST/WS 客户端（主循环）
sherpa-onnx>=1.13.4,<1.14   # ASR paraformer 中英双语流式 + TTS kokoro-multi-lang 推理
onnxruntime>=1.22,<1.30     # TextCNN 15 类意图分类器（本仓 nlu_data/ 随镜像）
numpy>=2,<3                 # 音频缓冲/重采样/模型 IO（研究轮实证 2.4 与上两者兼容）
pypinyin==0.55.0            # fast_path 拼音模糊匹配（"平推车"→"平推窗"）
zeroconf==0.132.2           # mDNS 广播 _huijian-voice._tcp（对齐网关加载项版本）
opuslib-next==1.3.1         # 裸 opus 编解码（协议钉死 16k/mono/60ms；import 名=opus）
