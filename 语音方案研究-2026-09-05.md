# 慧尖语音方案研究（加载项内支持语音 · 仅提供方案，未改代码）

> 日期：2026-09-05 ｜ 状态：调研定稿待决策
> 目标硬件：HA 主机 4 核 / 8GB / 120GB SSD
> 涉及资产：慧尖加载项（本仓）、ESP32 音箱固件（E:\AI\0513gujian）、小智服务器部署文档（E:\备份\小智服务器相关\061701）、配网小程序（E:\AI\ha-yy\weichat-huijian-hz）、已下载模型库（本目录 asr/nlu/tts）

---

## 0. 结论（TL;DR）

**可行，且大部分积木已经存在。** 推荐架构：

新建一个**独立姊妹加载项「慧尖语音」（huijian_voice）**（同仓库发布，不并入现有 42MB 网关加载项），内含：

| 环节 | 选型 | 依据 |
|---|---|---|
| 设备接入协议 | **兼容小智 WebSocket 协议**（WS :8000 + OTA :8003） | 固件（xiaozhi-esp32 深度 fork）已稳定实现，**改服务器地址零重刷**（BLE CMD12 写 NVS） |
| VAD | SileroVAD（随 server） | CPU 开销可忽略 |
| ASR | **sherpa-onnx 流式 Paraformer 中英 int8**（encoder.int8 158MB + decoder.int8 68MB） | 你已下载；正是小智文档在 4C8G 上验证过的同款 provider `SherpaParaformerASR.py` |
| NLU 快通道 | fast_path 正则 + TextCNN 级联（已有）＋ **Klar NLU 可选增强**（Rust、MIT、~50MB RAM） | 设备控制 <1s，不经 LLM |
| LLM | **云 API（OpenAI 兼容，DeepSeek/通义）**，本地 Qwen2.5-0.5b 仅可选降级 | 8GB 全机余量 ~4-5GB，LLM 本地化是最大内存变量 |
| TTS | **sherpa-onnx + Kokoro-82M multi-lang ONNX**（含 zm_/zf_ 中文音色，q8 ~92MB） | CPU 实时、~1GB、Apache-2.0；CosyVoice3-0.5B（19GB/PyTorch/GPU）与 nemotron（5.7GB/NeMo）明确**不进加载项** |
| HA 桥 | Supervisor token（homeassistant_api 模式，现有加载项已趟通）REST/WS | 控制**全部** HA 实体 + 场景激活 + **语音建场景**（写 storage scenes，可持久，见 §4.3） |
| 设备在 HA 呈现 | 固件自带 **ESPHome native API :6053（Noise PSK）** → 自动创建 ESPHome 条目 | 音箱=标准 HA 设备（音量/静音/播报/放音全部免费获得） |

**资源预算**：语音加载项稳态 **≈2.5-3GB RAM**（ASR 0.6G + TTS 1.0G + server 0.3G + VAD/杂项 0.3G + 余量），2×2 线程封顶；HAOS+Core 约 2.5-3GB → 8GB 机器总占用 ≈6GB，**成立**。磁盘：核心模型 ~550MB，加载项镜像 ~1GB（对比现在 42MB——安装体验对策见 §7）。

**端到端体验**：说"打开办公室筒灯" → 唤醒(本地) → 上行 Opus 16k → VAD/ASR 流式 → fast_path 命中 → HA REST `light.turn_on` → Kokoro 合成"已打开" → **<1s**；复杂/创建类走 LLM 2-6s。

---

## 1. 现状资产盘点（四仓 + 模型库，调研实锤）

### 1.1 慧尖网关加载项（本仓，v1.7.11）
Mosquitto@2022、零conf mDNS 广播（`huijian-mqtt._mqtt._tcp`）、nginx ingress@8099、小程序 WS 网关@9001、`/api/ha/` nginx 代理注入 SUPERVISOR_TOKEN、fast_auto_discovery 代理（"等待条目+重放"模式）、双架构 ghcr 预构建镜像 + CI 全套。**这些模式语音加载项全部可复制。**

### 1.2 ESP32 音箱固件（0513gujian）
- **78/xiaozhi-esp32 的深度定制 fork**（README 自述"为 Home Assistant 打造的 AI 智能音箱"，ESPHome 2025.6.3 内嵌合入）。
- 语音通道：小智 **WebSocket 协议**（hello/listen/tts/stt，Opus 上行 16k/60ms，`main/protocols/websocket_protocol.cc`）+ MQTT/UDP 备选（NVS `mqtt.endpoint` 存在时**优先级高于 WS**——重定向测试前必须清 NVS 残留，`application.cc:381-389`）。
- **私有扩展协议**（我们自己的服务端必须实现）：`speaker_tts`（让设备播报指定文本）、`speaker_order`/`front_speaker_order`（下发指令/先问后执行）、tts 状态 `play_start/play_stop/listen_start`（服务端驱动回合控制）。
- **设备端还常驻一个 ESPHome 原生 API 设备**（TCP :6053，Noise 加密）：暴露 `wakeup_button`、`mic_switch`、`volume_number`、`play_voice_text`、`execute_command_text`、`ask_and_execute_command_text`、`huijian_media_player`（可播任意 URL 音频）。→ **HA 官方 ESPHome 集成可以直接连它**，播报/媒体/静音/音量全走标准 HA 实体。
- 配网：**纯 BLE 私有协议**（微信小程序通道）：CMD10=WiFi、**CMD12=服务器地址四件套**（wsUrl+wsToken+otaUrl+mqttInfo → NVS + 重启）、**CMD20=HA 入驻**（存 Noise PSK + 向 `haUrl+/api/huijian-ai/setup/qrcode` POST `{host,port:6053,noise_psk,mcp_endpoint,speak_id}`）。
- 本地唤醒词 esp-sr **你好小智**（WakeNet9s），设备端 NS+VAD；屏 SSD1306；S3 bread-compact-wifi 当前构建。
- **无 mDNS**（固件不广播不浏览）；BLE 写入仅 CRC **无鉴权**（安全面见 §10）。
- 关键点：本 fork 的 OTA 响应**不再携带 ws/mqtt 配置**（只认 NVS）→ 一旦被 BLE 指到局域网服务器，**不会被厂商云抢回去**（粘滞，`ota.cc:139-153`）。

### 1.3 小智服务器文档集（E:\备份\...\061701）
xinnan-tech/**xiaozhi-esp32-server**，你已在 4C8G 云服务器上跑通全栈并有大量定制：
- 单模块模式（纯 Python server + `data/.config.yaml`，**无 MySQL/Redis/智控台**）与全模块（8002 Java web）两条路都有文档。
- 现成 provider：`SherpaParaformerASR.py`（就是你 yy/asr 里那个模型）、`SherpaOnnxTTS.py`（Kokoro multi-lang，sid=45 中文女声等）；**53 条中文热词纠错表**（统灯→筒灯、内导/内岛/内到→**内倒**、汇江→**慧尖**——完全为你的产品线调过）。
- **语音快速通道 fast_path.py**：六级级联（复杂句守卫→动作正则→区域前缀→**HA 场景缓存匹配（60s 刷新、REST 直连 HA）**→TextCNN-ONNX 15 类意图→OOS 兜底 LLM），命中即 MCP/REST 执行，**<1s**；"宁漏判不误执行"原则。
- 已定案教训：fast_path 注入与 provider 靠 docker-cp 进容器，**"容器重建后注入丢失"是文档自认的第一失败面**——加载项镜像化恰好根治。
- 全模块 RAM 账（文档原表）：OS 1G+MySQL 0.5G+Redis 0.1G+server 0.3G+web 0.3G+…剩余 ~3.8G 给 LLM——注意该表**未计 ASR/TTS 常驻**，真实余量更小 → 佐证"LLM 上云"。

### 1.4 配网小程序（weichat-huijian-hz）
BLE 语音设备配网全流程已存在（扫描 huijian/esp32/xiaozhi 广播名 → 握手 → WiFi → **CMD12 下发小智服务器** → CMD20 下发 HA/Noise）。缺口只有两个：
1. wsUrl/otaUrl **全部取自微信云环境变量（公网值）**，没有"用户家里 HA 局域网 IP"来源；
2. 微信 mDNS **不透传 TXT、iOS 不支持**（LoRa 侧已知坑，端口恒写死 9001 的教训）。
→ 对策：二维码携带完整 URL（ha-connect 已有扫码能力）+ 手输兜底，**BLE 链路本身零协议改动**（详见 §5）。

### 1.5 yy 模型库体检（34GB 里只留 ~550MB）
| 模型 | 体量 | CPU 可用性 | 判定 |
|---|---|---|---|
| sherpa 流式 Paraformer 中英 | int8 226MB | ✅ 流式、低延迟，文档同款 | **用（ASR）** |
| sherpa Paraformer zh int8 离线包 | 218MB(tar) | ✅ | 备选（非流式兜底） |
| nemotron-3.5-asr 0.6B | 5.7GB NeMo/safetensors | ❌ NeMo 栈重、CPU 慢 | 弃 |
| Kokoro-82M v1.0 ONNX | q8 92MB / fp32 310MB | ✅ ~1GB RAM，含中文音色 zf_xiaoxiao/xiaobei、zm_yunyang 等 12 个 | **用（TTS）** |
| MOSS-TTS-Nano-100M ONNX | 660MB | ✅ torch-free，但输出 48k 立体声需重采样转 Opus | 候选（音质更好时替换） |
| Fun-CosyVoice3-0.5B | **19GB** PyTorch | ❌ 需 GPU；克隆音色是卖点但不该进加载项 | 弃（云端增值项） |
| Qwen3-TTS-12Hz-0.6B | 3GB PyTorch | ⚠️ 97ms 流式宣称，但 CPU RTF 未验证 | 观察池 |
| klar-ha-nlu | 42MB | ✅ Rust 单二进制，几十 MB RAM，67 语言含 zh-CN | **可选（第二层确定性 NLU）**，见 §4.1 |

---

## 2. 硬件约束与预算表（4C / 8G / 120G）

| 进程 | RAM | CPU 线程 | 备注 |
|---|---|---|---|
| HAOS+Core+Supervisor+慧尖网关加载项 | ~2.5-3GB | 弹性 | 现状 |
| 慧尖语音 server（小智单模块裁剪） | ~250-350MB | 0.5 | asyncio |
| SileroVAD | ~30MB | 0.2 | |
| ASR Paraformer int8 | ~500-700MB | **≤2** | 流式，会话期常驻；可选项"懒加载"（首次唤醒才载入，冷启动 +1.5s） |
| TTS Kokoro（quantized） | ~900MB-1GB | **≤2** | 22.05k → 重采样 24k Opus 下行 |
| LLM | **0（云 API）** | 0 | DeepSeek/通义；本地降级方案 qwen2.5:0.5b（ollama 独立加载项，1-1.5GB，选项开关） |
| 合计语音侧 | **≈2.6-3GB** | 峰值 ~3 核 | memory 上限建议在 config.yaml 声明保护 HA |

- 并发口径：**1-2 台音箱同时对话**（家庭典型）；N 台待机只占 ~20MB/台。
- 120GB SSD：镜像 ~1GB + /data（模型 550MB 或缓存、配置、日志、音乐）<3GB ✅。
- 真机必测项（aarch64 NAS 弱核上 sherpa 线程性价比、Kokoro RTF、4 核竞争下 fast_path 端到端时延）列入 M1 验收。

---

## 3. 推荐架构（方案 A：加载项内自建语音服务器）

```
                 ┌─────────────────── HA 主机（4C8G）───────────────────┐
  LoRa网关 ══MQTT:2022══▶ [慧尖网关加载项]──┐                            │
                 │   Mosquitto + 窗控集成 + nginx:8099   │              │
                 │                                      ▼              │
                 │        HA Core（REST/WS）◀── SUPERVISOR_TOKEN ──┐   │
                 │            ▲                                   │   │
                 │   [慧尖语音加载项 huijian_voice]  host_network  │   │
                 │    OTA :8003 ◀───(设备配网/注册)                │   │
                 │    WS  :8000 ◀────── 小智协议 Opus 16k ──┐      │   │
                 │    ├ VAD → ASR(sherpa Paraformer)        │      │   │
                 │    ├ fast_path（正则/TextCNN/场景缓存）   │      │   │
                 │    ├ [可选] Klar NLU(:10520 第二加载项)   │      │   │
                 │    ├ Agent：OpenAI 兼容 LLM（云/本地）    │      │   │
                 │    │   + HA tools（状态查询/服务调用/     │      │   │
                 │    │     场景激活/场景创建/自动化生成）    │      │   │
                 │    └ TTS(sherpa Kokoro 中文) ──Opus──▶   │      │   │
                 │                                          │      │   │
                 │   ESPHome 集成 ◀══ TCP:6053+Noise ══╗     │      │   │
                 └────────────────────────────────────╨─────┴──────┘
   ┌─────────────┐   BLE CMD10 WiFi / CMD12 服务器URL / CMD20 Noise+入驻
   │ ESP32 音箱   │◀──（微信小程序配网，全部现成协议，仅地址来源改造）
   │ 你好小智唤醒 │──▶ ws://<ha-ip>:8000/xiaozhi/v1/ + http://<ha-ip>:8003
   └─────────────┘    └─ 设备在 HA = ESPHome media_player + 按钮/音量/静音实体
```

### 3.1 实现路线二选一（方案 A 内部）
- **A1 打包 xiaozhi-esp32-server 单模块（推荐先走）**：fork 上游，把你在云服务器验证过的 `SherpaParaformerASR.py`/`SherpaOnnxTTS.py`/`fast_path.py` 固化进镜像（根治 docker-cp 丢失），砍掉 MySQL/Redis/智控台（单模块 `.config.yaml` 全配置化，由加载项 options 渲染生成），加一个薄 HTTP 层（OTA/注册/配对二维码）。**工作量最小、与现有客户云端体验完全一致**；维护=跟随上游 fork。
- **A2 自研轻量语音服务器（P2 演进）**：只做小智 WS 协议 + 慧尖扩展（`speaker_tts` 等）+ sherpa FFI + HA 桥，全自主可控（Go/Rust/Python 皆可，现有 aiohttp 班底够用）。A1 验证产品后按需切换，设备侧无感（协议同构）。
- 明确**不做**：把全模块（Java+MySQL+Redis+web）搬进加载项——RAM 与运维都不允许。

### 3.2 为什么保留小智 WS 协议而不是改走 HA Assist/Wyoming
- 固件麦克风采集→服务端音频链是小智协议实现的；换 ESPHome Voice Assistant 要重刷固件、丢屏幕表情/音乐/BLE 配网/小程序整合（方案 C 详述为何不推荐）。
- 设备**同时**说两种语言（小智 WS 管对话，ESPHome API 管被 HA 控制）——两全：对话不依赖 HA，HA 播报不依赖对话服务器。

---

## 4. 三大能力设计

### 4.1 语音控制"HA 中所有设备及场景"
三层意图级联（全部 LAN 本地）：
1. **fast_path 正则/TextCNN**（已有、已含窗控词汇与"内倒"纠错）：开/关/调亮度/调色温/窗帘百分比/空调温度模式/**场景触发词**（60s 缓存自 HA）→ 直接执行，<1s。
2. **确定性 NLU（可选叠加 Klar 加载项）**：Klar 固定意图集**原生含 cover 位置（HassSetPosition）**、场景激活、13 个域白名单；扩展点是"自定义语句 → 绑定 HA 已注册 intent"——我们的窗控集成可注册产品意图（`内倒 tilt_inward`、`平开模式 wind_lock_flat` 等），中文语句**确定性**绑定，零 LLM 参与。注意其 zh 语料是生成级（第二梯队），商用需按慧尖话术做 overlay 精调；MIT 可自由内置/分发。
3. **LLM function-calling 兜底**：tools = `list_entities/get_state/call_service/activate_scene/create_scene/create_automation/query_history`——**任意 HA 实体、任意服务调用理论上都可达**（这就是"所有设备"的完备性保证）；闲聊/天气/多轮也在这层。
   - 房间上下文：MAC→区域映射（语音加载项 options，参照云 DB macMap 思路本地化），prompt 注入"你在{客厅}"，"开灯"只影响本房间。
   - 慧尖开窗器天然就是 `cover` 实体——零额外适配进语音。

### 4.2 语音激活场景
已存在（fast_path `_check_scene`），产品化=把触发词缓存从"REST 拉取 60s"升级为 HA WS 订阅推送，其余不动。

### 4.3 语音创建场景（真正的增量）
两种语义都要覆盖：
- **快照型（高频、必须稳）**："把现在客厅的样子保存为『观影模式』"
  1. GET `/api/states` → 按说话设备所在区域过滤可控实体（排除 battery/传感器等只读域）；
  2. 组装 `entities` 字典 → **POST `/api/config/scene/config/{id}`**（HA UI/storage 场景写接口，ha-mcp 项目在用、由 `scenes.yaml` 托管、**重启持久**；`scene.create` 服务是临时内存场景，官方 issue #133912 实锤重启即丢，**不能当实现**）；
  3. 播报确认 + 立即可被 4.2 激活。
  ⚠️ 两项 M3 验收前置：目标 HA 版本（现网 2026.1.3）该端点可用性与字段格式实测；Supervisor token 经 `/core/api` 的权限级别是否满足 config 写（HA 对 config REST 要求管理员——Supervisor 映射的 hassio 用户通常 admin，须实证，符合"我方真栈验证"规矩）。
  **兜底方案（100% 可控）**：语音加载项自持"慧尖场景库"→ 由慧尖集成的自定义 scene 平台暴露成 `scene.*` 实体（在 HA 里同样可激活/自动化引用，只是不出现在 设置→场景 的原生编辑器）。主推 config REST，兜底自定义平台，二选一按 M3 实测结果定。
- **编排型（可选，P1+）**："每天晚上六点拉窗帘留阳台灯" → LLM 产出 automation JSON → 音箱/网页二次确认 → POST `/api/config/automation/config/{id}`（UI 自动化的 REST 写入是 HA 稳定老能力）。

---

## 5. 发现与配网闭环（"在 HA 局域网中被自动发现"）

固件无 mDNS，所以发现链路设计为**手机侧发现 + BLE 写入 + 服务端登记**，全部复用现成积木：

```
① 语音加载项 zeroconf 广播 `_huijian-voice._tcp`（实例 huijian-voice，mdns_publisher 同款代码复制）
② 小程序（或 HA App）：wx.startLocalServiceDiscovery 发现 HA 主机 IP
   —— 微信 mDNS 拿不到 TXT、iOS 不支持（LoRa 侧已知）→ 双兜底：
   a) 首选：加载项 ingress Web UI 显示【配对二维码】= 完整 URL+token
      （http://<lan-ip>:8003/xiaozhi/ota/?key=xxx）——小程序「扫一扫」已存在（ha-connect）
   b) 次选：扫不到就手输 IP:端口（IP 可点 mDNS 结果自动填）
③ 小程序把 LAN 地址经 BLE CMD12 原样写入（wsUrl=http→同源的 ws://<ip>:8000/xiaozhi/v1/、
   otaUrl、token）——固件协议零改动
④ 设备重启连 OTA:8003 → 语音加载项应答（activation 省略即免绑；firmware 段可下发我们自己的 OTA 更新）
   → 设备连 WS:8000 hello（Device-Id=MAC）→ 加载项登记"慧尖音箱"
⑤ 入驻 HA：小程序 CMD20 把 haUrl+NoisePSK 写给设备 → 设备 POST
   `/api/huijian-ai/setup/qrcode`（host、port:6053、noise_psk、speak_id）——
   **该端点由语音加载项实现**（nginx /api/ha/ 同款代理模式反向往 HA 注入 discovery），
   随后自动创建 ESPHome 配置条目 → HA「设备与服务」出现音箱，语音播报/media_player 立即可用
   （沿用 v1.7.11 定式：条目须用户点击确认，代发起不代配对）
⑥ 存量公网设备迁移：同一 CMD12 覆写即可（NVS 粘滞，§1.2），无感回迁局域网
```

端口策略：8000/8003 为**约定值**（xiaozhi 社区习惯，便于调试）；但设备侧 URL 含端口整体下发，**二维码路径端口可自由改**，mDNS 手输兜底同理——避免重蹈 9001 写死覆辙。
安全：OTA/WS 握手校验配对 token（加载项生成、放 NVS 的正是它）；BLE 无鉴权风险见 §10。

---

## 6. 数据与持久化（/data 布局）

```
/data/config.yaml          # 由加载项 options 渲染（selected_module、providers、端口、token）
/data/models/paraformer/   # 或镜像内置（§7 分发策略）
/data/models/kokoro/
/data/voices/              # 预留：音色切换（Kokoro sid → 智控台同款映射进 options）
/data/music/               # 预留：本地音乐（文档已有 playAudioUrl 链）
/data/scenes/              # 兜底自建场景库
/data/state/devices.json   # MAC↔名称↔房间↔最后在线
/data/logs/
```
options（config.yaml schema）：LLM 模式(cloud/local)+base_url+api_key(password 型)+model、语音音色/语速、fast_path 开关、区域映射、配对 token、懒加载开关、(P2)Klar 端点。

---

## 7. 加载项形态与分发（不并入现有加载项的理由 + 安装体验对策）

**独立加载项**（同仓新增 `huijian_voice/`，repository.yaml 挂两个）理由：
- 镜像体积：42MB → ~1GB 会把 ghcr 拉取从"3-4 分钟"拉到"半小时+"（家宽实测 84-216KB/s 的历史教训直接适用）；
- 生命周期解耦：语音重启不影响窗控；崩溃隔离；
- Supervisor 可对语音加载项单独设 memory cap，防止推理峰值挤死 HA；
- opt-in：低配机器客户只装网关加载项，高配客户加语音——商业分层清晰。

**模型分发对策（关键决策点）**：
- 方案 a：模型烧进镜像（+550MB）——确定性最高，无首启网络依赖；
- 方案 b（推荐）：**轻镜像 + 首启多源下载**（modelscope 主源【国内 CDN，你 yy 目录里那批就是这么下的】→ hf-mirror 备源 → 失败则提示把模型包放 /data/models 的"导入路径"，Web UI 显示进度）；模型包与加载项版本解耦，升级不重下；
- 双架构：onnxruntime/opus 均有 aarch64+amd64 轮子，沿用现有 CI builder 矩阵；NAS ARM 核性能入 M1 实测。

---

## 8. 复用清单 vs 新建清单

**直接复用（已存在，别重造）**
| 资产 | 位置 |
|---|---|
| 小智 WS/MQTT 协议、唤醒、ESPHome API、BLE CMD10/12/20 | 固件（零改动） |
| BLE 配网栈 + CMD12 服务器下发 + 扫码 ha-connect | 小程序（仅加"局域网模式"来源） |
| SherpaParaformerASR / SherpaOnnxTTS / fast_path（正则+TextCNN+53 纠错表+场景缓存） | 云服务器现网定制（随镜像固化） |
| zeroconf mDNS 广播、/api/ha/ nginx 代理、CI 双架构镜像流、快速发现"等待条目+确认卡片"模式 | 本仓网关加载项 |
| 语音模型（Paraformer int8、Kokoro 多语） | 本目录已下载验证 |

**新建（工作量全在这里）**
1. `huijian_voice` 加载项壳：Dockerfile/run.sh/config.yaml/options→.config.yaml 渲染/ingress 页面；
2. OTA :8003 端点（应答 Device-Id 登记 + activation 可省略 + firmware 段预留）；
3. 注册端点 `/api/huijian-ai/setup/qrcode` 落点 + ESPHome 条目自动创建（含用户确认卡片）；
4. HA 桥 tools（云/本 LLM function-calling 用）：states/services/scene 激活/**scene 创建**/automation 创建；
5. 小程序"局域网语音配网"入口（二维码/手输 IP → 复用既有 CMD12/20 写通道）+ `tools/cmd12_writer.py` 调试器（M0 就要）；
6. Web UI：设备列表/在线状态/配对二维码/模型设置/日志。

---

## 9. 分阶段路线图

| 阶段 | 内容 | 验收（真栈实证，我方完成，不拿客户当测试） |
|---|---|---|
| **M0 局域网可行性实锤**（1-2 天，零仓内代码） | 用现有单模块镜像+xiaozhi-server 在 HA 同网机器跑起来；`cmd12_writer.py`(bleak) 把一台在库 S3 指向 LAN；HA 手动建 ESPHome 条目（IP+PSK） | "打开办公室筒灯"<1s 执行；"开窗到 50%"动作到位；HA 播报经音箱出声；断外网全链路仍通（LLM 除外） |
| **M1 语音加载项 MVP** | 镜像化（A1 路线）、options、/data 持久、OTA、配对二维码页、模型首启下载 | 全新 HA 一键装；CI 双架构绿；4C8G 真机 RAM/CPU 曲线达标；重启/升级后配置与设备不丢 |
| **M2 HA 深桥** | 注册端点+ESPHome 自动条目（确认卡片）、MAC→房间映射、多音箱、fast_path 场景缓存改 WS 订阅 | 两台音箱分属两房间，"开灯"各管各屋；断连重连、加载项升级设备无感 |
| **M3 语音建场景** | 快照→写 storage 场景→激活闭环；LLM 编排型自动化（确认流） | "把现在客厅保存为观影模式"→ 设置→场景 里可见可编辑、重启仍在；权限/版本实测记录归档 |
| **M4 配网闭环 + 可选项** | 小程序局域网模式发版；Klar 叠加（内倒/平开自定义意图）；ollama 本地 LLM 选项 | 存量公网设备批量迁回 LAN；100 句慧尖话术集 NLU 准确率报告 |
| **P2 以后** | 视觉/vision_explain、CosyVoice 音色克隆（云端增值、可选走公网）、RagFlow 知识库（**只配得上独立服务器，不进 8G 机**）、跨网 MQTT-UDP | — |

---

## 10. 风险与待验证清单

| # | 风险 | 缓解 |
|---|---|---|
| 1 | 8GB 真实余量比纸面更紧（HA recorder/插件膨胀） | LLM 强制云默认；ASR/TTS 懒加载+独立开关；Supervisor memory limit；Web UI 显示内存水位 |
| 2 | 4 核 CPU 竞争（ASR+TTS 各 2 线程 + HA） | 线程封顶 2+2、优先级、M1 真机压测；NAS aarch64 单核弱 → 给出"推荐 x86 小主机"产品口径 |
| 3 | `config/scene` 写接口的 HA 版本/权限门槛 | M3 首要实测；兜底=自建 scene 平台实体（§4.3） |
| 4 | BLE 无鉴权：近距离任何人可改服务器 URL/OTA 任意 URL（固件现状） | 短期=配网指引"首次使用现场配对"+OTA 服务器只回签名固件；中期=固件帧加 token 校验（与 LoRa 侧 set_token 同思路），列入固件 TODO |
| 5 | 设备粘滞 LAN：语音加载项挂了=音箱离线（fork 不再被云抢回） | 加载项看门狗+HA 通知"语音服务离线"；Web UI 一键回切公网（重写 CMD12） |
| 6 | 上游 xiaozhi-server fork 维护成本（协议随固件演进） | A1→A2 内生化路线；协议文档固件仓自带（docs/websocket.md 等） |
| 7 | 微信 mDNS 限制（TXT/iOS） | 二维码携带完整 URL 为主路径（已设计）；长期看 HA App 配网页替代 |
| 8 | 隐私口径：文本出网（云 LLM），音频始终不出户 | 文档如实写；本地 LLM 选项给"全离线"客户 |
| 9 | 唤醒词仍是"你好小智" | 品牌决策：esp-sr 定制唤醒模型（"你好慧尖"）需训练/采购，P2 |
| 10 | 热词依赖：Kokoro/Paraformer 对"筒灯/内倒/慧尖"误识 | 53 条纠错表随镜像固化并按客户话术扩充（机制已有） |

---

## 11. 备选方案对比（为何不选）

| | A 加载项内语音服务器（推荐） | B 云服务器保留+薄桥接 | C 重刷 ESPHome Voice Assistant + HA Assist | D 公网小智云+MCP 接 HA |
|---|---|---|---|---|
| 局域网离线可用 | ✅ | ❌（断外网瘫） | ✅ | ❌ |
| 8G 机资源 | ✅（3GB 预算内） | ✅ | ⚠️ 需 whisper+piper 替换（中文效果↓或同样重）| ✅ |
| 保留现有固件/屏幕/音乐/小程序 | ✅ | ✅ | ❌ 全重做 | ✅ |
| "在加载项中支持语音"诉求 | ✅ 原生满足 | ❌ 不在加载项 | ✅ 但换生态 | ❌ |
| 语音建场景 | §4.3 双方案 | 需 LLM 写权限在云侧（公网暴露） | Assist 生态无现成（要自建 tool） | 云侧难落地 |
| 工作量 | 中（复用 80% 积木） | 小 | 大（固件+配网+UX 重做） | 最小但天花板低 |

> C 的长期观察点：HA 原生语音生态（Voice PE/Wyoming/openWakeWord/Assist LLM）增速很快；但中文确定性控制与屏幕交互上，你手里的小智栈目前是更成熟的现成品。A 与 C 并不互斥——设备已双栈（小智 WS + ESPHome API），未来若走 C 只换"耳朵和嘴"，HA 侧实体模型不变。

---

## 12. 需要拍板的问题

1. **LLM 默认云端还是本地？**（推荐：云默认 + 本地 0.5b 选项；涉及隐私话术与客户群定价）
2. **语音建场景第一版范围**：只做"保存当前状态"？还是含"定时自动化编排"？（推荐先快照型）
3. **独立加载项确认**（推荐），以及模型分发取 a（烧镜像）还是 b（首启下载，推荐）？
4. **配网入口**：小程序加"局域网模式"，还是引导用 HA App/网页配网页替代手机端？
5. **音箱硬件/唤醒词品牌**（"你好小智"是否够用一年？）——影响固件 P2 排期。
6. 现有云服务器上的定制（fast_path/纠错表/两个 provider 源码）能否归拢进本仓库管理？——M1 的输入件。

---

### 附：本次调研引用（关键代码位置速查）
- 固件：`main/ota.cc:43-47`（OTA URL 只认 NVS）、`main/protocols/websocket_protocol.cc:221-246`（hello/Opus16k）、`main/ble/ble_manager.cc:455-585`（CMD12/CMD20）、`main/esphome/esphome_device.cc:169-310`（6053+Noise+实体）、`application.cc:381-389`（MQTT 优先于 WS）
- 小程序：`pages/wifi-config/wifi-config.js:1578-1604`（CMD12 五段 TLV）、`pages/ha-connect/ha-connect.js:615-681`（CMD20）、`cloudfunctions/haControl/index.js:423-439`（URL 来自云环境变量）、`utils/gateway-discovery.js:164-169`（mDNS TXT 不透传）
- 小智文档：一键部署指南（单模块/全模块、端口表、RAM 账）、ASR 部署指南（Paraformer int8/Kokoro sid）、语音快速通道方案（六级级联/阈值/docker-cp 丢失之痛）、添加本地 LLM 指南（4C8G Qwen2.5 1.5b/0.5b 实测）
- HA 场景持久化：ha-mcp `tools_config_scenes.py`（`config/scene/config/{id}` 由托管 scenes.yaml 支撑）；`scene.create` 服务为临时态（home-assistant/core#133912、社区"does not save on reboot"实锤）
- 本加载项可复制模式：`huijian_mqtt_broker/mdns_publisher.py`、`gateway_discovery_proxy.py`、run.sh nginx `/api/ha/` 注入段、CI 双架构镜像链
