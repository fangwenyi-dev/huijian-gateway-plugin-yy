# 小智协议子集 — 服务器端精确契约（供 huijian_voice 加载项 WS 服务器实现）

> 提取日期：2026-09-08。方法：以 HA 集成（消费端）源码为**唯一权威**逐行反推；
> 开源 xiaozhi-esp32-server 仅作交叉印证（凡两者有差异处，以消费端为准并显式标注 ⚠）。
>
> 路径缩写：
> - `CC/` = `E:/AI/huijian-gateway-plugin-yy/yyjicheng/custom_components/huijian_ai/`（消费端）
> - `UP/` = `E:/AI/xiaozhifuwuqi/xiaozhi-esp32-server-main/main/xiaozhi-server/`（上游服务器，参考）
> - `UPD/` = `E:/AI/xiaozhi-docker/xiaozhi-server/`（本地 docker 部署副本）
>
> 消费端核心事实：**三条 WS 通道 = 三条独立连接**（stt_endpoint / tts_endpoint / llm_endpoint 各自
> 懒连、各自一个 WsTransport 实例：`CC/huijian/stt_transport.py:29`、`tts_transport.py:30`、
> `llm_transport.py:29`），共用基类 `CC/huijian/ws_transport.py`。

---

## 1. 握手 hello

### 服务器端必须实现的行为
1. **接受不带任何身份的裸 WS 连接**：hello 帧不含 device_id/token 等身份字段，且 **TTS 通道和 LLM 通道根本不发 hello**（见 §3/§4），服务器**禁止**把"收到 hello"作为处理任何后续帧的前置门。
2. **容忍重复 hello**：STT 通道**每次识别请求前都重发一次 hello**（同一连接可收到 N 次 hello），服务器必须幂等处理。
3. 收到 hello 后**应当**回一个 `{"type":"hello", ...}` JSON 帧（字段见下），但**客户端完全不消费、不等待该响应**——hello 响应帧会被三个通道的过滤器当作"未知类型"跳过（`stt_transport.py:42`、`tts_transport.py:45-48`、`llm_transport.py:43-46`）。**任何通道都不得依赖客户端确认握手。**
4. hello 响应**必须** `type:"hello"`。禁止在 hello 响应位置回 `type:"stt"`/`type:"tts"`/`state:"stop"`/`state:"end"` 等语义帧，否则会被误当作业务结果消耗（STT 通道取 `text`、TTS 通道遇 `state=stop` 直接结束）。
5. `session_id` 字段可选（建议带上以便日志对账）：消费端四个文件中**没有任何代码读取 session_id**（全库 grep 仅 `mcp_transport.py:107` 一个无关局部变量命中）。
6. 客户端 hello 的 `audio_params` 声明了上行参数 16k/mono/opus-60ms；服务器应答中 echo 回去即可，客户端不解析。
7. hello 无应用层超时/重试；连接层超时见 §6。

### 证据
- 客户端 hello 帧构造（字段全集，仅此 4 键）：`CC/huijian/ws_transport.py:282-295`
- 只在 STT 请求前发送、每请求一次：`CC/stt.py:100`（`send_hello`）；`CC/tts.py`、`CC/conversation.py` 全文无 send_hello 调用
- 客户端对 hello 响应的忽略路径：`CC/huijian/stt_transport.py:42`（type 不在 ["stt","tts"] 即继续等）、`CC/huijian/tts_transport.py:45-48`（非 bytes、非 stop → 记日志跳过）、`CC/huijian/llm_transport.py:43-46`（state≠end 且 type≠text → continue）
- 上游印证（welcome 形状 = 配置节 xiaozhi + session_id + echo audio_params）：`UP/core/connection.py:248-249`、`UP/core/handle/helloHandle.py:42-60`（:49 echo、:60 发送）、`UP/config.yaml:88-97`（`xiaozhi: {type:hello, version:1, transport:websocket, audio_params:{format:opus, sample_rate:24000, channels:1, frame_duration:60}}`）
- ⚠ 与上游差异：上游 welcome 的 `audio_params.sample_rate` 出厂值是 **24000**（下行 TTS 采样率，`UP/core/connection.py:252`），而本消费端**硬解码 16000**（§3）——我们的服务器下行必须出 16k，不能照抄上游 24k 默认。

---

## 2. STT 通道（stt_endpoint 连接）

### 完整请求时序（客户端发出，服务器按序接收）
1. `hello` 帧（§1，JSON）— `CC/stt.py:100`
2. `{"type":"listen","state":"start"}`（**无 mode、无 sample_rate 字段**，就这两个键）— `CC/stt.py:102`
3. N 个 **WS 二进制帧**，每帧 = **一个裸 opus packet**（无 Ogg 封装、无长度头、无序号）— `CC/stt.py:103-104`（`wav_to_opus` 逐个 encode → `send_message(chunk)` → `ws_transport.py:307-308` `send_bytes`）
4. `{"type":"listen","state":"stop"}` — `CC/stt.py:106`

### 音频参数钉死位置（16k / mono / 60ms / 每帧一个 packet）
- 通道侧类常量：`CC/stt.py:31-34`（`opus_channels=1, opus_sample_rate=16000, opus_frame_duration=60, frame_samples=960`）
- 实际编码函数默认参（stt.py:103 调用时不传参，全部用默认）：`CC/huijian/audio.py:127`（`wav_to_opus(stream, sample_rate=16000, channels=1, frame_duration=60)`）
- 每帧 PCM 切片 → 一次 `encoder.encode` → yield 一个 packet：`CC/huijian/audio.py:128-129`（frame_bytes=960×1×2=1920 B）、`audio.py:146-151`；尾部零填充后多出一帧：`audio.py:152-154`
- 编码器为 `opuslib.Encoder(16000, 1, APPLICATION_AUDIO)`：`CC/huijian/audio.py:130`（注：`CC/stt.py:54-56` 另有一个 APPLICATION_VOIP 编码器实例，**在识别路径上是死代码**，仅说明上行为标准 opus 裸包，无 FEC 特殊要求）

### 服务器应答（必须）
1. 对每次 `state=stop` **恰好回一帧**：`{"type":"stt","text":"<整句识别结果>"}`。
   - 客户端等待逻辑：`CC/stt.py:109`（`await_message(60)`）→ 过滤器 `CC/huijian/stt_transport.py:37-44`——**取到的第一个 `type ∈ {"stt","tts"}` 帧即作为最终结果并 break**（只等 **1 帧**）。
   - 结果取值：`CC/stt.py:111-112` 读 `resp.text`；无论收到什么，最终都返回 `SpeechResult(text, SUCCESS)`（`CC/stt.py:113`）。
2. **禁止在本通道发任何二进制帧**：bytes 无 `.type` 属性，会令 `stt_transport.py:42` 抛 AttributeError，直接打死消息接收协程（`ws_transport.py:261-263` re-raise → 断连重连）。
3. **禁止发中间/部分结果**（partial）：第一帧 stt 即被视为最终结果。流式 ASR 的内部中间态必须服务器自己吞掉，只在 stop 后发终稿。
4. **禁止在 stt 帧之前/之后顺带发 tts 业务帧**：`type:"tts"` 在过滤器里与 `"stt"` 同权（`stt_transport.py:42`），先发 `{"type":"tts","state":"start"}` 会被当作结果消耗且其 `text` 缺失 → 识别恒得 None；stop 之后残留的帧会滞留到下一次请求（见 §6「滞留帧」）。**STT 端点 = 纯 ASR，一问一答单帧。**
5. 即使静音/失败也**必须**回 stt 帧（text 可为空串）；不回的话客户端干等 60s 后按超时处理（`stt_transport.py:47-48` yield error Dict → text=None → 仍然 SUCCESS(None)）。
6. 超时预算：从 stop 起 **60 秒**内必须出帧（`stt.py:109` 显式传 60；`stt_transport.py:37` 默认亦 60）。
7. 允许在 stt 帧之前存在 hello 响应帧（会被过滤器跳过，不占结果位）。

---

## 3. TTS 通道（tts_endpoint 连接）

### 请求帧（核实结论：**无 hello，首帧直发 detect ——属实**）
- `CC/tts.py:63-75`：`ensure_connected()` 后直接 `send_message({"type":"tts","state":"detect","text":message})`——整个 `tts.py` 无 `send_hello` 调用；服务器必须以"连接后第一帧即 tts detect"为合法输入。
- 请求不含格式/语速/音色参数（`options` 仅 HA 本地用于选输出容器 mp3/wav，`CC/tts.py:68`，不上线路）。

### 服务器应答序列（必须）
1. **一串 WS 二进制帧**：每帧 = 一个**裸 opus packet，16 kHz、单声道、60 ms（960 样本）**。
   - 解码器硬编码位置：`CC/tts.py:27-30`（`opus_sample_rate=16000, opus_channels=1, opus_frame_duration=60, opus_frame_samples=960`）；`CC/tts.py:78`（`opuslib.Decoder(16000, 1)`，每请求新建）；`CC/tts.py:82`（`decoder.decode(resp, 960)`——**packet>60ms/帧长不符会解码异常**，仅记日志丢帧 `CC/tts.py:83-84`，导致音频缺损）。
   - ⚠ 禁止照抄上游默认 24k 下行（§1）；Kokoro 22.05k 等模型必须重采样到 16k 再编码。
   - 裸包语义印证（上游同款）：`UP/core/handle/sendAudioHandle.py:259`（逐 packet `websocket.send(opus_packet)`）。
2. **末帧 = `{"type":"tts","state":"stop"}`（或任意 type、只要 `state=="stop"`）**：客户端判定结束的唯一依据是 `CC/huijian/tts_transport.py:43-46`——bytes 一律 yield；JSON 遇 `state=="stop"` **break**；其余 JSON 一律记"unknown"跳过。
3. **中间文本帧允许但无用**：`sentence_start`/`sentence_end` 等 JSON 帧不会打断接收（落到 `tts_transport.py:47-48` 仅打日志），若加载项 UI 需要对账可发，但**不要**用文本帧承载结束语义。
4. **禁止发含 `error` 键的 JSON 帧**：`CC/tts.py:88-89` 对非 bytes 帧检查 `resp.error`，存在即抛 RuntimeError 使整个合成失败。
5. **stop 之后禁止再发任何帧**（含二进制）：客户端生成器已退出，无人消费 → 阻塞接收协程 → 污染下一请求（§6「滞留帧」）。
6. **整个流（首帧音频 + stop）必须在一共 60 秒内完成**：`tts_transport.py:38`（`anyio.fail_after(60)` 包住整条消费循环，`CC/tts.py:79` 调用未传超时用默认 60）。即：**TTS 产出快于 1× 实时即可，但文本很长时必须流式、边合成边发**，总时长超 60s 客户端超时（超时 yield 的 error Dict 还会触发第 4 条的 RuntimeError 抛错路径）。
7. 客户端拿到 PCM 后的转码参数（证明其认定输出流是 16k/mono s16le）：`CC/tts.py:93-104`（ffmpeg `-f s16le -ar 16000 -ac 1 → mp3/wav`）。

### 一问一答映射
- 每条 `tts detect` → [binary×N, stop] 一次完整流；同连接可串行多次请求（HA 的 TTS 调用可能反复使用同一 transport 实例，`tts_transport.py:22-25` 缓存复用）。服务器须按序响应、不交错。

---

## 4. LLM / conversation 通道（llm_endpoint 连接）

### 请求帧
- `{"type":"listen","state":"detect","text":"<用户原话>"}` — `CC/conversation.py:88-96`。
  - 注意实现细节：conversation.py 里是 `send_message(json.dumps(...))`（**发的是字符串**，`CC/huijian/ws_transport.py:302-306` 对 str 直接 `send_str`），与 stt 的 dict 序列化路径不同但**上线等价**（dict 路径带 `ensure_ascii=False`，`ws_transport.py:303`；str 路径为 conversation.py 自行 dumps 的默认 ensure_ascii=True——仅转义差异）。
  - 同样**无 hello**（conversation.py 全文无 send_hello）。

### 服务器应答序列（必须）——聚合逻辑在 `CC/huijian/llm_transport.py:37-54`
消费规则逐行：
- `llm_transport.py:42` 循环收帧；**`:43-44` 任何 `state=="end"` 的帧 → 立即终止聚合**（该判断先于 type 检查，所以 end 帧的 type 理论上可为任意值，建议仍 `type:"text"`）；
- `:45-46` `type != "text"` 的帧 → 忽略（continue）；
- `:47-48` `state == "start"` → **content 清空重计**；
- `:49-50` `state == "sentence_end"` 且 **`data` 字段是字符串** → `content += data`。
- `:51` 结束后 yield 一帧 `{"role":"assistant","content":<所有 sentence_end.data 顺序拼接>}`。

由此得服务器行为清单：
1. 应答帧全部为 **JSON 文本帧**，**禁止二进制**（bytes 无 `.state` → `llm_transport.py:43` AttributeError 打死接收协程，同 §2 第 2 条后果）。
2. 推荐序列（每条 detect 一次）：
   `{"type":"text","state":"start"}` →
   每句一条 `{"type":"text","state":"sentence_end","data":"<整句完整文本>"}`（可有多条）→
   `{"type":"text","state":"end"}`。
3. **载荷字段名是 `data`，不是 `text`**（`llm_transport.py:49` 读 `data.data`；若把句子放进 `text` 字段会被 `isinstance(...,str)` 判 None 跳过 → 最终回复空串）。
4. `sentence_start` 增量帧**不被消费**（无句子累加逻辑；只有 `sentence_end` 的 data 会入结果）——服务器可省略，或为调试 UI 发送（会被 `:45/:49` 逻辑静默丢弃/忽略）。
5. `state=="end"` 帧**必须有且恰好一次**，它是唯一终止信号；没有它客户端等 `fail_after` 超时（内层默认 180s，`llm_transport.py:37`）。
6. 超时预算（外层更严）：`CC/conversation.py:73-80` 用 `fail_after(60)` 包住整条 await——**全流（start→end）须约 60 秒内完成**，超时客户端向 ChatLog 注入 `{"error":"抱歉，AI 响应超时，请重试"}`（`conversation.py:80`），对话以错误文案收尾。
7. 每条应答的第一个 `start` 帧兼具"重置"作用：若上一条残留帧滞留队列，start 不能救回已被提前 break 的结果（见 §7 打断与 §6 滞留帧），服务器须保证**一问一答不交错、响应及时完成**。

### ChatLog 如何消费 delta
- `CC/conversation.py:97-99`：`chat_log.async_add_delta_content_stream(self.entity_id, 流)`。由于 `llm_transport` 是**先内部聚合、一次性 yield 单条**完整消息（`llm_transport.py:51`），HA ChatLog 实际收到的是**单元素流**：一条 `{"role":"assistant","content":"全文"}`（无逐 token 增量、无早流式播报）。超时路径元素为 `{"error": "..."}`（`conversation.py:80`），HA 按错误解析。即：**服务器不需要（也无法）向该通道做逐字流式；分句的粒度只影响服务器内部对 TTS/日志的利用，不影响本客户端。**

---

## 5. 鉴权

### 服务器端必须实现的行为
1. **鉴权信息只可能出现在 URL query**：消费端 `ws_connect(self.endpoint)` **不传任何自定义 header**（`CC/huijian/ws_transport.py:188`；mcp 同构 `mcp_transport.py:132`），endpoint 从配置条目原样取出（`stt_transport.py:17` / `tts_transport.py:18` / `llm_transport.py:17`）。服务器不得要求 `Authorization`/`device-id`/`client-id` header。
2. ⚠ **禁止照抄上游的连接准入门**：上游对无 device-id（header 或 query 都没有）的连接直接回文本再关闭（`UP/core/websocket_server.py:83-98`），huijian 三通道连接**永远不带 device-id**，若保留该逻辑会 100% 拒掉 HA——**本服务器必须匿名可连**。
3. 凭据键名约定：慧尖云注入的完整 URL 形如 `ws://…/xiaozhi/v1/?token=<…>`（盘点文档 §1 引实测 setup_data；config 存储链 `CC/huijian/http.py:58-85` → `CC/config_flow.py:260-262`）。加载项自产自用，query 键可自定（建议沿用 `token=`），但**解析 query 时必须宽容**（未知键忽略、缺 token 在 LAN 形态放行）。
4. **401 语义（鉴权拒绝的唯一正确姿势）= HTTP 握手层返回 401 状态码**（在 WebSocket upgrade 完成之前）。客户端行为：`WSServerHandshakeError.status==401` → **永久停止重连 + 清空该 endpoint 配置 + （mcp 通道额外）触发 HA reauth UI**——`CC/huijian/ws_transport.py:210-215`、`clear_endpoint_from_data :70-83`、`CC/huijian/mcp_transport.py:157-166`。
5. **1008 = "被顶号"**：客户端收到 close code 1008 后**禁止重连**（`CC/huijian/ws_transport.py:269-271`，注释"被顶号后，禁止重连"）。服务器只应在实现"同 token 单会话强制互踢"策略时用 1008 踢**旧**连接；其他结束一律 1000/1001，否则 HA 侧该端点永久失活直到用户重新配置。
6. 认证失败也允许用握手 403/5xx（等价于"失败→走通用重连退避"），但只有 401 会精确命中"停重连+清配置+reauth"路径——语义与慧尖云对齐用 401。

---

## 6. 连接生命周期

### 服务器端必须知道/实现的行为
1. **懒连**：三条连接都是首次业务调用时才建立；`ensure_connected` 同步等待连接就绪，**上限 15 s**（150×0.1s 轮询，`CC/huijian/ws_transport.py:114-123`），HTTP 握手 timeout `connect=60, total=None`（`ws_transport.py:179`）。服务器应保证握手快速返回（别在 upgrade 前做慢查询，>15s 该次 STT/TTS/LLM 直接失败返回，`stt.py:97-99`）。
2. **断线自动重连、退避 3→60 s**：`run_connection_loop` 每次失败后 sleep `max(min(60, n*5), 3)`（3,5,10,15,…,60,60…），**连接成功即清零计数**——`CC/huijian/ws_transport.py:130-151, 196`。因此：服务器主动断开（正常码）≈ 客户端最多 3~4 s 后自愈重连；但**断开后首个请求可能等待退避时间**，频繁"每请求断连"会拖垮时延——**服务器不要在每次应答后关闭连接**。
3. **空闲 180 s 自关 = 纯客户端行为**：`_idle_timeout=180`（`ws_transport.py:38`）；监视任务每 30 s 检查，超时即 `should_reconnect=False` + cancel（优雅关闭 ws）（`ws_transport.py:223-241`）。活动时钟在**收到任何帧**（`ws_transport.py:249-250`）、**发出任何消息**（`:276`）、建连时（`:195`）刷新。→ 服务器视角：静默 ≥3 分钟可能看到客户端 Close(1000)，随时准备接受随后的懒连重建；服务器自己**不需要**做 180s 回收（上游的 120+60s 二道超时 `UP/core/connection.py:198-199, 1453+` 仅作参考，若加载项加空闲回收应 ≥180s 或用正常码，让客户端无感重连）。
4. **55 s ping 是客户端发的、且是标准 WebSocket 协议层 ping**（非 JSON）：`_heartbeat_task` 每 55 s `ws.ping()`（`CC/huijian/ws_transport.py:340-352`）。**服务器只需在协议层回 PONG——所有主流 WS 库（aiohttp/websockets）自动完成，应用层零实现**。客户端不发应用层 `{"type":"ping"}`；若服务器想额外实现 JSON ping/pong（上游 `UP/core/handle/textHandler/pingMessageHandler.py:24-42`），注意 `type:"pong"` 会被三通道过滤器静默跳过（安全），但**别用 pong 帧夹带业务字段**。
5. **滞留帧禁忌**（关键实现约束）：客户端收发经 0 缓冲 anyio memory stream（`ws_transport.py:127-128`），某请求超时/取消后，服务器后续到达的帧会**卡住接收协程并顺延到下一次请求的第一个 await_message**——可能直接命中上一条回答的 `end`/`stop` 残余，导致新请求秒回空结果。服务器必须：**每请求的应答在客户端超时前自然结束；被打断时（见 §7）立刻停止发送、绝不补发孤儿帧。**
6. 停机路径：HA 卸载条目/移除集成时客户端主动 `stop()` → `ws.close()` 正常码（`ws_transport.py:354-375`；各 transport `async_remove_entry`，如 `stt_transport.py:50-60`）。服务器按普通断连清理会话状态即可。

---

## 7. abort / 打断

### 事实
- **消费端从不发送任何 abort 帧**：`CC/stt.py`、`CC/tts.py`、`CC/conversation.py`、`CC/huijian/ws_transport.py`（含全部三通道）grep `abort/Abort/cancel` **零功能命中**。既没有 `{"type":"listen","state":"detect","text":"Abort"}`，也没有专用 `{"type":"abort"}`。
- conversation 打断（barge-in / HA 取消）在协议上的表现 = **什么都不发**，只是客户端停止读取响应流（协程被取消，后续服务器帧落入 §6 第 5 条的滞留队列）。
- 上游（供参考、非本客户端行为）：存在 `type:"abort"` 处理器（`UP/core/handle/textMessageType.py:7`；`UP/core/handle/abortHandle.py:16-20` 收到后清队列并回 `{"type":"tts","state":"stop"}`），官方测试页在唤醒词打断时发 `{"type":"abort","reason":"wake_word_detected"}`（`UP/test/js/core/network/websocket.js:504-510`）。

### 服务器端必须实现的行为
1. **不要依赖客户端通知来停止推流**（它不会发）。当同一连接上收到**新的**请求帧（`tts detect` 或 `listen detect`）时，服务器必须**立即作废上一轮未完的应答流**（丢弃排队音频/文本，不再发旧流的 `stop`/`end`），然后只按新请求流式应答。
2. 建议（可选，纯增强兼容，不改变契约）：接受 `{"type":"abort"}` → 停当前流并按当前通道收尾语义回一帧（TTS 通道回 `state=stop`，LLM 通道回 `state=end`，STT 通道回空 `stt`），使未来改固件/改客户端时可直接使用；当前 HA 客户端不会触发。
3. 禁止实现"被打断后向客户端追问/发错误帧"——客户端对意外 JSON 帧的处理是静默跳过或（带 error 键时）抛错（§2/§3/§6）。

---

## 8. 端点 URL 路径约定

### 事实链（消费端不拼路径）
1. config_flow **不构造 URL、无手填 endpoint 字段**：三个 endpoint 是小程序配网时 POST 给 HA 的 `setup_data` 里的**完整 URL 字符串**，逐键原样写入 config entry——`CC/config_flow.py:253-263`（`llm_endpoint/stt_endpoint/tts_endpoint` 三键 :260-262）；写入侧视图 `CC/huijian/http.py:54-85`（`POST /api/huijian-ai/setup/qrcode`，:66 收 JSON、:84 暂存、:197-204 等待循环）。
2. 各通道原样取用：`stt_transport.py:17` / `tts_transport.py:18` / `llm_transport.py:17`（`entry.data.get(...)`）→ 原样 `ws_connect`（`ws_transport.py:188`）。**query/路径/header 附加行为一概没有。**
3. 平台按端点键有无挂载：`CC/__init__.py:63-68`（有 llm_endpoint 才建 conversation 平台，STT/TTS 同理）。

### 路径约定（现状与推荐）
- 慧尖云注入格式（实测样例见盘点文档 §1）：`ws://<host>:<port>/xiaozhi/v1/?token=<…>`。
- 上游服务器路由印证：自动生成的 WS 地址即 `ws://{ip}:{port}/xiaozhi/v1/`（`UP/core/http_server.py:33`）；`UP/config.yaml` `server.websocket: ws://你的ip或者域名:端口号/xiaozhi/v1/`（server 段内注释示例）。
- **注意：上游是"单一路径承载全功能"**（hello/listen/binary/abort 同一连接，响应走 stt+tts+二进制混合流），而 huijian 消费端把协议**拆成三条专功能连接**——三条连接**服务器侧必须能区分通道身份**（响应策略完全不同：STT=单帧文本、TTS=二进制流+stop、LLM=text 分句+end，互相冲突）。区分手段二选一：
  1. **（推荐）三条不同 URL** 由配网方注入，如 `/xiaozhi/v1/?channel=stt|tts|llm` 或三个路径；加载项同时控制 URL 注入，零歧义。
  2. 单 URL + 首帧嗅探绑定通道（hello/listen-start→STT 会话；tts-detect→TTS；listen-detect→LLM）——可行但复杂，且被 §6 滞留帧语义放大，不建议 MVP 采用。
- LAN 落地提醒：当前插件版本**没有手填 endpoint 的 UI**（端点只能由配网 POST 注入，盘点 §6-D1 已列为改造点）；`huijian_voice` 上线需配套改造插件或提供注入通道，此为集成侧工作项，不影响本契约。

---

## 帧样例（四条完整消息序列伪样例）

以下 `TX:`=客户端发出，`RX:`=服务器应答。JSON 均为一行 TEXT 帧；`<bin:...>`= WS 二进制帧。

### A. STT 会话（stt_endpoint 连接，可多次复用，节选两个请求）

```
# ---- 打开连接（URL: ws://192.168.x.x:8017/xiaozhi/v1/?channel=stt&token=...）----
TX: {"type":"hello","version":1,"transport":"websocket","audio_params":{"format":"opus","sample_rate":16000,"channels":1,"frame_duration":60}}
RX: {"type":"hello","version":1,"transport":"websocket","session_id":"8f2f…uuid…","audio_params":{"format":"opus","sample_rate":16000,"channels":1,"frame_duration":60}}   # 可选；客户端会跳过
TX: {"type":"listen","state":"start"}
TX: <bin: 裸opus packet #1  (20ms/40ms/60ms 中钉 60ms→960样本@16k, s16 mono 编码产物, ~40-80B)>
TX: <bin: opus packet #2>
TX: <bin: … #N（音频结束；末帧可能是零填充帧）>
TX: {"type":"listen","state":"stop"}
RX: {"type":"stt","text":"把客厅的灯打开","session_id":"8f2f…"}          # 唯一允许的结果帧；此外不得再发任何东西
# ---- 客户端取到即结束本轮；180s 无活动则客户端自行 Close(1000)，下次请求再懒连 ----
```

### B. TTS 会话（tts_endpoint 连接）

```
# ---- 打开连接（首帧即业务，无 hello）----
TX: {"type":"tts","state":"detect","text":"好的，正在为您打开客厅的灯"}
RX: {"type":"tts","state":"start","session_id":"a1b2…"}                     # 可选（客户端仅记日志跳过）
RX: {"type":"tts","state":"sentence_start","text":"好的，正在为您打开客厅的灯","session_id":"a1b2…"}   # 可选（客户端忽略）
RX: <bin: 裸opus packet #1（16kHz mono 60ms → 解码必须=960样本）>
RX: <bin: opus packet #2>
RX: <bin: …（连续 N 帧，边合成边发，总时长<60s）>
RX: {"type":"tts","state":"stop","session_id":"a1b2…"}                      # 终止信号；此后本请求禁止再发任何帧
```

### C. LLM 会话（llm_endpoint 连接）

```
TX: {"type":"listen","state":"detect","text":"明天客厅要开什么场景来着？"}      # 无 hello
RX: {"type":"text","state":"start","session_id":"c3d4…"}                    # 触发客户端 content 清零
RX: {"type":"text","state":"sentence_start","data":"明天","session_id":"c3d4…"}   # 允许但不会被计入
RX: {"type":"text","state":"sentence_end","data":"明天客厅触发的是「离家归来」场景。","session_id":"c3d4…"}
RX: {"type":"text","state":"sentence_end","data":"需要我现在打开客厅灯吗？","session_id":"c3d4…"}
RX: {"type":"text","state":"end","session_id":"c3d4…"}                      # 唯一终止信号
# 客户端聚合结果 = 两条 sentence_end 的 data 拼接为单条 assistant 消息交给 ChatLog：
#   {"role":"assistant","content":"明天客厅触发的是「离家归来」场景。需要我现在打开客厅灯吗？"}
```

### D. 打断/异常时序（服务器侧自律，客户端不发任何帧）

```
# 场景：LLM 正在推 sentence_end ×k 时 HA 侧对话被取消 → 客户端停读。
# 服务器：不感知、不发 abort 响应；当该连接再次收到新 detect 帧时：
TX: {"type":"listen","state":"detect","text":"换一个"}
# 服务器必须：①丢弃上一轮未发完的队列（包括其遗留的 end 帧，绝不再发）；②从新请求的 start 帧开始完整应答。
```

---

## 未决清单（编码前需知晓 / 需实测钉版）

1. **LLM 通道 `data` 字段与现网慧尖云是否逐字一致**——本契约从消费端代码反推（`llm_transport.py:49` 读 `data.data`，`state ∈ {start, sentence_end, end}`）已是**满足客户端的充分必要条件**；但慧尖云实际是否还发 `sentence_start` 部分帧、是否附带 `session_id`/`emo` 等扩展键，盘点文档标注"M0 现网抓包钉版"未决。**影响**：加载项服务器只需满足上述最小集即可与 HA 互通。
2. **STT 过滤器为何接受 `type:"tts"` 作结果**（`stt_transport.py:42`）——疑似兼容某版云端把识别结果走 tts 帧回显。我们服务器**不要**这样做，用标准 `type:"stt"`；但若云端真机行为如此，抓包确认即可（不影响本实现）。
3. **aiohttp 对入站 PONG 帧是否进 `async for` 消息流**——影响 §6-3 空闲 180s 在"55s ping 正常往返"时是否真触发（若 PONG 刷新活动时钟则连接永不空闲自关）。这是客户端内部实现细节，**服务器侧两种结果都可接受**（只需支持随时被重连/随时断），但加载项若要精确模拟云端会话回收节奏需实测。
4. **同连接并发请求**：契约全部按"每连接串行一问一答"推定（HA 单实体管线基本串行，`stt_transport.py:22-25` 实例缓存复用同连接）。两条 assist 管线并发打同一 stt 实体的行为**未验证**（0 缓冲队列会互相吞帧）；服务器实现按串行设计即可，此为客户端固有限制。
5. **LAN 配网注入通道**：当前插件版本无法手填三 endpoint（只能由配网 POST 写入），`huijian_voice` 需搭配盘点 §6-D1 的插件改造或经 `/api/huijian-ai/setup/qrcode` 注入——属集成侧待办，不改变协议契约。
6. **token 键名**：消费端只保证"query 原样透传"，`token=` 取自盘点文档对现网 URL 的记录；上游云用 `authorization=`+`device-id`（`UP/core/websocket_server.py:103-106`）。加载项自产自销，键名自定并宽容解析即可。
7. **close code 1000 vs 1001**：客户端只对 1008（禁重连）与 401 握手态特判（§5），其余码一律退避重连；服务器空闲回收/重启用 1001 即可自愈，具体码值无契约约束。
