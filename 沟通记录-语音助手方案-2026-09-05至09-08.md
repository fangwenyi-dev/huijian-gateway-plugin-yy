# 沟通记录 · 慧尖语音助手方案（2026-09-05 至 2026-09-08）

> 本文件是语音方案研究的**对话与决策完整留痕**（记忆体），供后续任意会话直接接续，不必重读全史。
> 配套档案：`语音方案研究-2026-09-05.md`(v1) → `语音助手落地方案-v2.md` → `-v3.md` → **`-v4.md`（含 v4.1 修订，现行执行基线）** + `语音集成源码盘点-huijian_ai-2026-09-08.md`（事实契约）。
> 2026-09-08 本目录自 `E:\AI\huijian-gateway-plugin\huijian-gateway-plugin-yy` 迁至现址 `E:\AI\huijian-gateway-plugin-yy`（独立工作区）。

---

## 一、诉求演变时间线

| 日期 | 用户原话要点 | 产出 |
|---|---|---|
| 09-05 | 研究 yy 文件夹：加载项里能不能做语音方案，**先不修改代码只提供方案**；约束：HA=4核8G120G；要 LAN 自动发现的 ESP32 控制 HA 全部设备+语音创建场景；给了四个参考资产（固件 0513gujian、小智服务器 docs、小程序 weichat-huijian-hz、模型库 asr/tts/nlu） | v1 研究稿：资产盘点+模型适配表+架构 A-D 对比+风险 |
| 09-06 | 四点细化：①TTS/STT/NLU **默认全本地内置**（云为可配项），LLM 用户自配"默认不需要"；②默认配置即可添加助手、装完零必填、事后可改远程；③小程序职责=**只做配网+连接 HA**；④要求梳理完整语音流程（STT→NLU→[LLM]→TTS） | v2 落地方案：自研轻量服务器定案、A→H 管线状态机、2×2 NLU/LLM 矩阵、settings.json 配置体系、CMD12 配网链、M0-M4 |
| 09-08 上午 | 五点决策回复：①默认模型=yy 文件夹内的本地 STT/TTS，云用户自配；②**音色可选，默认你(agent)来定**；③fast_path.py 在 `E:\备份\小智服务器相关\documents` 自己去看；④项目定位=**语音控制 HA 中所有设备**（迁移问题不成立）；⑤意图合计看 documents + **"不是还有 klar nlu 吗"** | 挖出重大增量资产：fast_path.py 669 行全文+TextCNN14类训练全套(2MB)、15 工具规格书(意图集要求.md)、SFT 数据集 350+436 条、Houzzkit 4697 行总纲、**huijian_yuyin 老版 HA 桥集成**；v3 定稿（NLU 四层、执行走 /api/intent/handle、纠错表/阈值/NameError 三收编缺陷） |
| 09-08 下午 | 提供 `yyjicheng`=**语音集成最新源码**（"固件也需要跟着修改！"）；指令：先整理文档放 yy 目录 | 双路深读+一手核验 → 《源码盘点 huijian_ai》+《落地方案 v4》：模式 B 定主推、集成 7 补丁清单、固件 voice_assistant 移植清单、D1-D20 缺陷册 |
| 09-08 晚 | 三确认：①公网小智服务器**从未上线→彻底退役**（模式 A 全线取消）；②stt/tts/llm 三项都允许用户自配、默认本地；③问：**语音加载项单独做还是并入 LoRa 网关加载项？** | v4.1 修订入文档：**单独新建 `huijian_voice` 加载项**（五理由），融合只做体验层 |
| 09-08 今 | 目录迁至 `E:\AI\huijian-gateway-plugin-yy`，沟通记录存本记忆体 | 本文件 |

## 二、当前定案快照（v4.1，全部经用户确认或授权）

**架构**：固件(ESP32-S3, xiaozhi fork, 移植 ESPHome voice_assistant 组件) ↔ ESPHome API 6053 ↔ `huijian_ai` 集成(HA 原生卫星+14 意图+语音场景+传感器自动化+管理页，源码 `yyjicheng/`) ↔ 三 WS 端点(小智协议子集，消费端源码在手=协议无未知风险) ↔ **新加载项 `huijian_voice`**（STT=sherpa Paraformer 中英 int8 默认/Kokoro sid45 默认，云可切+故障回落；fast_path+TextCNN 本地 NLU；执行统一 `POST /api/intent/handle` 长时令牌；LLM 默认关，开=OpenAI 兼容[百炼/火山方舟慧尖SFT 模板/LAN ollama]；Klar NLU 引擎随镜像默认关）。公网小智服务器、MCP 通道、模式 A 全部取消。

**加载项形态定案（单独 vs 并入网关）→ 单独**，五理由：①镜像体积（ghcr 家宽实测 3-4min/42MB，语音栈并入翻倍）；②重启爆炸半径（语音操作不能带走 Mosquitto=全屋窗离线）；③发布节奏解耦（网关 v1.7.x 高频 vs 语音月级）；④8G 资源隔离/可停可卸；⑤产品边界（语音执行走 intent，控制 HA **全部**设备，纯 HA 无网关客户应能单装=独立 SKU）。体验层融合三件套：语音加载项 run.sh 自动落盘 huijian_ai 集成；网关 Web UI 加"语音助手"跳转卡片；同仓兄弟目录 `huijian_voice/`+CI 矩阵双镜像+同条 Release 覆盖。

**关键量化**：稳态 RAM ≈2.0-2.2GB(含 HA 管线侧 ~0.2 增量，总盘 <3GB 含网关)；延迟预算说完→开口 p50≈1.3s/p90≈2.0s；音色表 45 小北(默认女)/46-48 女/49-52 男/0·10 英文+语速 0.8-1.2；TTS 通道硬约束=输出 16kHz mono 60ms opus（Kokoro 22.05k 需重采样）；评测门禁=436 条数据集改造回归集（控制≥95%/查询≥90%/误执行 0，真机端到端）。

## 三、意图/NLU 定案要点
- NLU 级联（加载项内）：T0 正则+纠错表(53 条按文档重建) → T1 TextCNN 14 类(按类阈值) → 场景触发(trigger_index/60s 缓存) → 查询族(M1 新增，"客厅多少度"本地读回) → LLM 档(可选) → 固定兜底。宁漏判不误执行。
- fast_path 收编必修 3 缺陷：`_HA_REQUEST_TIMEOUT` 未定义(NameError)、备份版缺纠错表、阈值统一 0.7≠文档按类。
- 场景创建双轨：快照型("把现在客厅保存为观影模式"=live_context→组 actions→HassCreateVoiceScene，**无需 LLM**，M1)/编排型(LLM 档，当我说X就Y + 传感器自动化)。
- 窗户语义已演进：窗→cover 域 Turn* + 集成内自动路由 ControlWindow（"见窗 button"旧规范已被 intent_device_shared 自动转换取代，fast_path 收编时对齐）。

## 四、缺陷与待办账（开码前必读）
- **集成侧 D1-D20**（详见盘点 §6）：上生产硬门槛 = D2 九端点无鉴权、D18 binary_sensor 自动化创建成功但永不触发、D1 mcp_endpoint 强制非空、D17 ControlWindow 只吃 targets[0]、锁语义反转(话术端)等。
- **急**：yyjicheng `.git/config` origin 内嵌 GitHub PAT（`ghp_Lir…`）**待用户吊销**；Houzzkit 实施计划 §1.5 含明文 HA token/Samba 密码勿入库。
- **待用户**：①固件改造先行设备（在库 S3 开发板/量产批次，涉 PSRAM 差异）；②可访问的测试 HA 做 huijian_ai live 对账（只读）；③v4+盘点合并正式《产品设计说明书》的归档指令。
- **M0′ 三向拉通（1.5 周）未启动**：加载项 echo 服务×真集成联调 / 固件卫星原型 / fast_path 收编三修；金标准=真机"打开客厅灯"全链路 p90≤2s。

## 五、纪律备忘（本工作区沿用网关插件铁律）
先诊断后动手、改码必回归、未经用户明说"推送"不 push（双远端）、每 release 提版本号、验证必须我方真栈实证不拿客户当测试、审查用 dsh-review-loop 不用 review 工具。
