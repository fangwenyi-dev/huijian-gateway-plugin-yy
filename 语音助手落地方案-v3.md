# 慧尖语音助手 · 落地方案 v3（定稿执行版 · 2026-09-08）

> 本版吸收用户五点决策（2026-09-08），并合并本轮对 `E:\备份\小智服务器相关\documents`、
> `E:\备份\语音集成`、`E:\AI\ha-yy\xiaozhi`、`E:\AI\xiaozhifuwuqi` 的新调研。
> v2（2026-09-06）的骨架（独立加载项、自研语音服务器、LAN 配网闭环、资源预算）全部沿用，
> 本文只覆盖并修订其 §3/§4/§10；与 v2 冲突处以本文为准。

---

## 一、决策点落定（用户 2026-09-08 确认）

| # | 决策 | 定稿 |
|---|---|---|
| 1 | STT/TTS 默认 | **内置本地**：STT=`yy/asr/sherpa-onnx-streaming-paraformer-bilingual-zh-en`（int8，encoder 165MB+decoder 72MB）；TTS=`yy/tts/Kokoro-82M-v1.0-ONNX`（multi-lang，quantized 92MB）。云 STT/TTS 作为用户自配选项（provider 可切换 + 失败自动回落本地） |
| 2 | 音色可选 | 本地：Kokoro 中文 8 音色（sid 45–52）+ 英文，语速 0.8–1.2，UI 试听；云：按厂商音色表可选。**出厂默认 = sid 45 `zf_xiaobei` 中文女声**（依据：你们部署指南自标"推荐"、现网 `.config.yaml` 已用 `sid: 45`，与存量客户听感一致，零迁移成本） |
| 3 | fast_path | ✅ 已收编到研究目录：`documents/xunlian/core/handle/fast_path.py`（669 行全文已读）+ TextCNN 全套训练资产（见 §二） |
| 4 | 存量公网云客户 | **不设切换期限**：新方案定位=局域网语音控制 HA 全部设备（核心卖点）；公网云客户继续使用其现有部署，想回迁的走"重新配网 CMD12 覆写"自助迁移，产品不主动动他们的设备 |
| 5 | 意图合计 | 三层合并：TextCNN 14 类 + 正则（快速通道）∪ **15 工具意图面**（huijian_yuyin 已定义并部分实现）∪ **Klar NLU**（可选档，见 §四） |

---

## 二、本轮新发现的现成资产（比 v2 假设多出一大截）

| 资产 | 位置 | 状态与用途 |
|---|---|---|
| **fast_path.py 全文** | `documents/xunlian/core/handle/` | ✅ 收编。六级级联：纠错→守卫→正则→区域前缀→场景缓存→TextCNN。⚠️ 此备份版有 3 处已核实缺陷（§三） |
| **TextCNN 训练全套** | 同上目录：`intent.onnx`(11KB)+`intent.onnx.data`(1.9MB)+`vocab.json`(5.7KB)+`train.txt`(1649 句,fastText 格式)+`train_textcnn.py` | ✅ 模型仅 ~2MB，直接随镜像分发；训练脚本在手，扩类可复训 |
| **注入参考** | `receiveAudioHandle_injected.py`、`play_music.py` | ✅ 真实管线顺序实锤：ASR 定稿 → `try_fast_path` →（命中）`fast_path_speak` 早退 →（未命中）`handle_user_intent` →（再未命中）`startToChat` LLM——v2 §2 流程图与此一致 |
| **15 工具规格书** | `documents/意图数据集/意图集要求.md` | ✅ 慧尖意图面的权威定义：场景管理×4（Create/Trigger/Delete/ListVoiceScene）+ 设备控制×5（TurnDeviceOn/Off、**ControlWindow(button: open/close/pause/a)**、AdjustDeviceAttribute、SetDeviceMode）+ 快捷×2（HassLightSet、HassClimateSetTemperature）+ 查询广播×4（huijianGetLiveContext、GetDateTime、HassBroadcast、HassCancelAllTimers）。含窗户vs窗帘（见"窗"→button/ControlWindow，见"帘"→cover/Turn*）、错误处理、模糊追问全套规范 |
| **SFT 数据集** | `慧尖HA集成_SFT训练数据集_合并.jsonl`(350)、`慧尖HA集成完整数据集.jsonl`(436)、`工具domain对照表.jsonl`、`窗户domain强化学习.jsonl` | ✅ 双重用途：① LLM 可选档的 function-call 微调/系统提示词蓝本（火山方舟 SFT 格式已在 `documents/火山/` 备好）；② **直接改造成 NLU 评测集**（M1 验收 100 句语料的基础） |
| **huijian_yuyin HA 集成** | 备份：`E:\备份\语音集成\老版本\huijian_yuyin-hz`（manifest 2026.4.0；另有 060401.zip 待验）；计划文档指向 `e:\AI\ha-yy\huijian_yuyin-hz`（**当前盘已不存在**） | ⭐ 这就是 v2 里"要新建的 HA 桥"：ESPHome 集成 fork + 11 个 intent handler 已实现（intent_turn/window_operation/adjust_attribute/set_mode/set_cover_position/stop_cover/live_context/broadcast/cancel_timers/datetime/helper）+ `/api/huijian-ai/setup/qrcode` 入驻端点 + speakname 同步 + 二维码生成。⚠️ 老备份未见 4 个 VoiceScene 工具实现（可能在更新版）——**最新源码位置需要你确认（§八-Q1）** |
| **Houzzkit改造慧尖实施计划.md** | `documents/`，4697 行 | ✅ 总架构参考：BLE 协议详解(§五)、配网流程、意图系统(§六：HA 内置意图长表——timers/media/vacuum/… 已在 intent_* 文件实现对应项)、小程序对接(§七)。⚠️ 其 §1.5 含明文 HA token/Samba 密码——**此文档勿入 Git 仓库、勿放公开网盘**（卫生提醒，处理建议见 §八-Q4） |
| **小智服务器本地源码** | `e:\AI\ha-yy\xiaozhi\xiaozhi-esp32-server-main\main\xiaozhi-server`（含 manager-* 全模块）+ `E:\AI\xiaozhifuwuqi\`（deploy、SenseVoiceSmall 权重、**kokoro-multi-lang-v1_0 模型包**、mcp-endpoint-server 源码、FunASR 源码） | ✅ provider 移植与协议对照的权威参考；Kokoro 模型包本地已有一份（除 yy/tts 外） |
| **固件 ESPHome VoiceAssistant 管线** | `0513gujian/components/esphome/.../api_connection.cpp:1266 subscribe_voice_assistant` | ✅ 内嵌 ESPHome 框架带完整 Assist Satellite 协议面（当前 main/ 未实例化 voice_assistant 组件）→ **P2 预留**：若未来想要"设备直接进 HA Assist 管线"的原生档，固件侧协议通道已在 |

---

## 三、fast_path 收编时必须修的 3 个实锤缺陷（本备份版）

| # | 缺陷 | 证据 | 处置 |
|---|---|---|---|
| 1 | `_HA_REQUEST_TIMEOUT` 被引用（:626）但**全文件未定义** → REST 回退路径一触发即 NameError | grep 全文仅 1 处使用、0 处赋值（方案文档里"修复 REST 回退路径 NameError"正对应此坑，修复在现网容器，不在备份） | 收编时定义 `_HA_REQUEST_TIMEOUT = 10`；E2E 断言 REST 回退可达 |
| 2 | **本备份没有 `_ASR_CORRECTIONS` 纠错表**（53 条只存在于《语音快速通道方案.md》文档与现网容器） | 全文无该符号；grep "统灯" 仅命中 `_ACTION_PATTERNS` 的窗型词 | 以方案文档中的表为准重建 + 从现网容器 diff 校准（M0-0 动作） |
| 3 | 阈值与文档不符：代码统一 `prob<0.7→None`（:94），文档称按类动态（OOS 0.85/Scene 0.5） | 代码 vs 《快速通道方案》§nlu_predict | 收编时实现按类阈值表（config 可配），默认沿用文档值 |

其余收编注意：`call_mcp_endpoint_tool`/`call_mcp_tool` 依赖替换为 LAN 直连执行器（下节）；`_TOOL_NAME_MAP` 存在重复键（TurnDeviceOn 等）——Python 静默取后者，顺手清理；裸 `except: pass` 场景刷新（:50）收编时加日志。

---

## 四、修订后的核心架构（v2 骨架 + 本次发现）

### 4.1 执行通道：MCP 全删，走 HA 意图 REST
云架构里小智服务器隔着公网，靠 MCP endpoint 反向控制 HA；**LAN 内语音服务器与 HA 同机，整条 MCP 链（mcp-endpoint-server :8004 + WS 穿透）不再需要**：

```
fast_path / LLM tools / Klar → 统一 intent 计划
   → POST {HA}/api/intent/handle  {"name":"TurnDeviceOn","data":{"target":[{...}]}}
     （= huijian_yuyin 已注册的同名 intent —— 解析/匹配/歧义/窗户button域特判全部复用现实现）
   → 需回读结果时 GET /api/states → 生成总结句 → TTS
```
- `args` 的 JSON 形态沿用 fast_path 现有构造（`target:[{devices:[{domains,name}],area}]`），**执行器改造量 ≈ 换一个 transport 函数**；
- VoiceScene 四工具的落点：在**随加载项分发的 huijian_yuyin 精简版**里补齐实现（存储放加载项 /data 或 HA `.storage` 子集；trigger_phrase→intent 动作序列执行器）。场景触发词供 fast_path `_refresh_scenes` 拉取（同样换 intent/REST）。

### 4.2 huijian_yuyin 的角色收敛与瘦身（M1 工作项）
现集成=整个 ESPHome 官方集成的 fork（百级文件、含摄像头/蓝牙代理/仪表盘）。随语音加载项分发**精简版 `huijian_yuyin`（或更名 `huijian_bridge`）**，只保留：
1. 15 intent 注册 + 匹配辅助（intent_helper）；2. VoiceScene 存储与执行；3. 入驻端点（setup/qrcode、pair、device-info、speakname）——**CMD20 设备自动建条目**在这层完成（其内嵌 aioesphomeapi 客户端连设备 :6053 建 Noise 会话，音量/静音/播报实体）；4. `huijian_voice_utterance` 事件。
其云端中继（llm/stt/tts/ws/mcp transports = HouzzKit 时代遗产）在 LAN 模式关闭或删除。
> 保留"完整版共存"路径：已装公网方案（huijian 云）的客户 HA 上跑的是完整版，精简版加载项版与其**域名不同**（如 `huijian_bridge`）避免冲突——这是"存量不迁移"承诺的工程前提。

### 4.3 NLU 四层定案（回应"不是还有 klar nlu 吗"）

```
文本 → [T0 正则+纠错+区域/场景] →命中→ 执行（<50ms，慧尖词表全量）
     → [T1 TextCNN 14类→映射 intent]→命中→ 执行
     → [T2 Klar 引擎(可选,默认关)] →命中→ 执行（补：状态查询/定时器/扫地机/媒体泛化，本地确定性）
     → [T3 LLM(用户配置,默认关)] → 15 tools function-call（复杂表达/编排建场景/闲聊）
     → 全关/全 miss → 固定兜底话术
```
- **Klar 定位**：MIT Rust 引擎单二进制（~20-50MB RAM）随镜像分发，`127.0.0.1:10520 POST /api/v2/parse`，家图谱经 `POST /api/v2/home` 推送（用我们 ha_bridge 缓存，不依赖读 .storage）；意图结果映射到同一套 15 工具执行。默认 `nlu.klar.enabled=false`：① 项目极年轻（3 周公开史、V2 破坏性变更已发生一次）；② zh 语料为生成级需按慧尖话术做 overlay 精调（我们 M0 起积累 436 条评测集，正好用于其精调与验收）。开关放加载项设置页，文案"扩展本地理解（含查状态/定时器）"。
- 意图缺口盘点（T0-T2 默认档下）：**"窗户开多大/客厅多少度"这类查询默认不答**（复杂守卫里"多少/查询/状态"一律送 LLM）→ 两个选择：M2 前先在 T0 加"纯查询"正则族（状态读回播报），或让用户开 Klar。**建议：查询正则族进 M1**（低成本高频体验点，你 Q5 问的"查状态"由此本地闭环，不依赖 LLM/Klar）。
- 15 工具面 vs "控制 HA 中所有设备"：TurnDeviceOn/Off 的 domains 白名单（light/climate/fan/cover/media_player/humidifier/switch/lock/button）——**不在名单的域**（humidifier 之外的 sensor 型、water_heater、vacuum、alarm 等）由 T2/T3 承接（HA 内置意图长表 Houzzkit §6.1 已列实现映射）；`AdjustDeviceAttribute` 泛化参数（attribute/value）+ 未来 LLM 的 `ha_call_service` 万能工具作完备性兜底。

### 4.4 语音流程全图（v2 §2 微调两处）
v2 §2.1 主状态机不变；修订：
- C 段 STT 与 D 段之间**显式插入纠错表**（`_ASR_CORRECTIONS`+用户自定义表），云端 STT 结果同样过表；
- F 执行统一为 `POST /api/intent/handle`（含歧义时 huijian_yuyin intent 内已有的追问语义——"打开灯"缺区域→intent 侧返回澄清→服务器播报追问，与 Houzzkit §九规范一致）；
- 场景创建双轨定案：**触发型**（"当我说X就关卧室灯"）= HassCreateVoiceScene（LLM 或后续 fast 模板句均可用，M2）；**快照型**（"把现在客厅保存为观影模式"）= M1 先做：GET /api/states 区域过滤 → 组装 Turn*/Set* 动作数组 → HassCreateVoiceScene（**不需要 LLM，纯 NLU+REST 编排**）。旧稿"写 HA storage scenes"降为可选导出功能（P2，经 `/api/config/scene/config/{id}`，先过权限实测）。

### 4.5 配置增量（v2 §4.2 settings.json 之上）
```jsonc
"tts": {"provider":"local_kokoro", "sid":45, "speed":1.0,
         "voice_catalog":["45 小北(女·默认)","46 小妮(女)","47 小晓(女)","48 小伊(女)",
                          "49 云健(男)","50 云希(男)","52 云扬(男)","0/10 英文", …],
         "cloud": null /*{"provider":"bailian_cosyvoice"|"volcengine"|"openai_tts_compat",
                          "api_key":"***","voice":"…"}*/},
"stt": {"provider":"local_paraformer", "cloud": null /*{"provider":"bailian"|"openai_whisper_compat"|"volcengine"}*/},
"nlu": {"enabled":true, "klar":{"enabled":false}, "query_local":true /*M1 查询正则族*/},
"corrections_extra": [],
"llm": {"enabled":false, "openai_base_url":"", "api_key":"", "model":"",
         "preset":"huijian_sft_v1" /*火山方舟慧尖SFT模型可作预置端点模板*/}
```
Supervisor options 仅高级字段（端口/token/省电/模型管理）同 v2；Web UI 设置页三卡：识别 / 理解（含 Klar 开关与语料测试框）/ 播报（音色试听列表，逐条点播）。

---

## 五、意图总表（合并三源 · 执行面唯一）

| 层 | 载体 | 意图/工具 | 状态 |
|---|---|---|---|
| T0/T1 | fast_path 正则 + TextCNN **14 类**（AdjustBrightness/AdjustTemperature/ControlWindow/OpenCover/SceneTrigger/SetColorTemp/SetFanSpeed/SetMode{Auto,Cool,Dry,Fan,Heat}/TurnDevice{On,Off}）+ 守卫词表（当/如果/创建/场景/多少/查询/状态→上层；吗/？→上层） | →15 工具 | ✅ 模型与词表在手（含"窗开到50%/关一半/内倒/暂停/暖光冷光4000k/±温度±亮度/播放音乐/English 子集"实测语料） |
| T1.5 | 慧尖场景触发词表（`HassListVoiceScenes` 拉取缓存） | HassTriggerVoiceScene | 触发面✅；**List/Create/Delete 需补进精简版集成** |
| T2 | Klar 引擎（可选默认关） | HassTurnOn/Off/LightSet/SetPosition/GetState/Climate…/timers/lists/vacuum/media + 自定义语句绑定（内倒/平开） | 引擎在手；zh overlay 精调为工作量 |
| T3 | LLM（可选默认关，OpenAI 兼容） | **15 工具全量** + `huijianGetLiveContext` 先行查询再创建的编排流程（§6.2 规范） | 工具规格+436 条样本在手；火山 SFT 可训慧尖专属模型（预置模板） |
| 兜底 | 固定话术 | — | ✅ |

## 六、里程碑修订（v2 §8 基础上改三处）
- **M0 增加"版本 diff"**：现网容器内的 fast_path 现态 vs 本备份（纠错表/NameError/阈值三处），拉一次 `docker cp` 存档进 `语音资产归仓/`（一次性人工动作，见 §八-Q2）；其余同 v2（协议 spike/延迟/试听/4C8G 曲线）。
- **M1 增加**：① huijian_yuyin 精简版（15 intent + VoiceScene 补齐 + 入驻端点 + utterance 事件）；② 快照型建场景（无 LLM）；③ 查询正则族；④ 436 条数据集改造成 `tests/intent_eval_436.yaml` 回归集（验收线：T0+T1 在"控制类"子集 ≥95%、"查询类"≥90%、误执行 0）。
- **M4 云档位明确三家**：阿里百炼（DashScope，Paraformer/CosyVoice 同源，你已有通道）、火山引擎（ASR/TTS + 方舟 SFT 慧尖模型）、OpenAI 兼容（whisper/TTS，通用兜底）。Klar 视客户"查状态/定时器"需求从 M4 提前。

## 七、安全与合规增量
1. Houzzkit 计划文档含明文凭据 → 归仓前先脱敏（§八-Q4）；
2. intent REST 面只监听 127.0.0.1（经 HA http 组件的 `/api/` 本身有 auth，走 supervisor token）；
3. 语音服务器对设备端口（8000/8003）全部 token 鉴权；BLE 无鉴权风险与缓解同 v2 §7；
4. 用户自定义纠错表/场景名注入 intent 执行面时做参数校验（防"场景名带模板注入"）。

## 八、遗留问题（只剩 4 个，都不阻塞 M0）
- **Q1（最重要）**：`huijian_yuyin` **最新版**源码在哪？当前盘只有老备份（2026.4.0，`E:\备份\语音集成\老版本`+`060401.zip`待验）；计划文档所指 `e:\AI\ha-yy\huijian_yuyin-hz` 已不在此盘。若客户 HA 上跑着更新版（可能已含 VoiceScene 实现），M1 需以客户机现版为基线收编，否则以老备份为基线。→ 需要你提供路径，或给我任一在跑的客户/测试 HA 的访问方式（我方真栈实证规矩）。
- Q2：现网云服务器容器内 `fast_path.py`/`SherpaParaformerASR.py`/`SherpaOnnxTTS.py`/纠错表现态导出归仓一次（M0 前置，你操作或给我入口）。
- Q3：Klar 首发客户要不要开默认？（工程上我建议：随镜像携带引擎、默认关、UI 一键开——零成本试销。）
- Q4：Houzzkit 计划文档入库方式（建议：脱敏后放仓库 `docs/` 作架构参考；或干脆不进仓，只在备份盘留档）。

---
### 配套文件索引（本轮新增证据）
- `documents/xunlian/core/handle/fast_path.py:103-148`（正则全表）`:194-199`（场景工具映射）`:388-402`（慧尖 4 工具+HA 内置意图混合分派）`:465-488`（args 构造协议）`:547-562`（工具名映射）`:601-653`（MCP→REST 双通道）`:655-669`（fast_path_speak 回合注入）
- `documents/xunlian/intent.classes.json`（14 类）/`train.txt`/`train_textcnn.py`/`vocab.json`
- `documents/意图数据集/意图集要求.md`（15 工具+窗户button规范+场景四操作+错误/模糊处理）
- `E:\备份\语音集成\老版本\huijian_yuyin-hz/custom_components/huijian_yuyin/`（manifest 2026.4.0；intent_*.py×11；huijian/http.py 端点组）
- `documents/Houzzkit改造慧尖实施计划.md`（§五 BLE/配网、§六意图系统、§七小程序对接；§1.5 含明文凭据勿外传）
- 固件 ESPHome VoiceAssistant 管线：`components/esphome/esphome/components/api/api_connection.cpp:1266-1272`
